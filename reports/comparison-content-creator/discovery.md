# Phase 0 discovery — comparison-content-creator (reused by competitor-monitor-auto, 2026-09-18)

- **BASE_URL**: https://www.themodernwallet.com (astro.config.mjs `site`, matches src/data/site.ts SITE.url)
- **Framework**: Astro 4 static export + React islands (@astrojs/react), sitemap via @astrojs/sitemap with `isNoindexedPath` filter (src/data/noindex.ts). No server-side redirects (static).
- **Business/brand name**: `ModernWallet` (src/data/site.ts SITE.name). Operator: Layer3Labs. Author byline: Jonathan Velez (AUTHOR const) — YMYL author for finance content.
- **Comparison content format**: `src/data/comparisons.ts` exports `ComparisonEntry[]` as `COMPARISONS` (plus segment files `comparisons-business.ts`, `comparisons-business-structure.ts` imported in). Route: `/compare/<slug>/` via `src/pages/compare/[slug].astro`. Interface fields: slug, title(≤60), metaDescription(≤160), targetKeyword, optionA, optionB, segment?, h1, introText, comparisonTable.rows[{dimension,a,b}], verdict, sections[{heading,content}], faqs[], sources?, relatedComparisons?, calculatorLinks?, draft?, updated?. New entries are pushed directly into the `COMPARISONS` array (no separate `*-new.ts` auto-merge file in this repo).
- **Guide format**: `src/data/guides.ts` (huge, 2.4MB) is the main guide store; segment files exist too (guides-business.ts, guides-self-employed.ts). Competitor-monitor Phase 3 routes NEW pages here per keyword-gap-pass/phase-3-new-content.md conventions (guide vs spoke vs comparison vs roundup, chosen by page type).
- **Other content stores**: spokes-*.ts (many verticals), roundups.ts (+ roundups-business.ts, roundups-self-employed.ts), calculators.ts (hub copy), templates.ts, se-hubs.ts, state-overrides.ts.
- **Entity universe**: personal-finance calculators/tools (auto loan, mortgage, retirement, investing, net worth, budget, mileage, freelance rate, portfolio, real estate, probate, estate planning, elder care, tax resolution, S-corp, business financing/factoring/LOC/MCA) — guides, spokes, roundups, comparisons across these verticals.
- **Existing comparisons dedup set**: full `COMPARISONS` array in src/data/comparisons.ts (+ comparisons-business*.ts) — read at dedup time (Phase 2 of this skill) via grep/slug scan.
- **Service/lead mapping**: free calculators drive affiliate/lead-gen roundups (best lenders, best cards, etc.) — comparison + guide pages support the calculator hubs and roundup CTAs.
- **Publish conventions**: sitemap generated by @astrojs/sitemap integration at build time (no static public/sitemap*.xml checked into repo — astro.config.mjs `sitemap()` integration handles it; filtered by noindex.ts). IndexNow key present: `public/dc557f6bfced447aa1a71771d8a0d24a.txt`. Git push flow: commit on current branch, deploy via fetch/rebase/push to main (auto-deploy repo). Typecheck/build: `npm run build` (astro build && node scripts/check-bundle-size.mjs); `npx tsc --noEmit` also available (tsconfig.json present).
- **Reusable scripts**: `scripts/submit_indexnow.py` (IndexNow submission), `scripts/lib/content_gen.py` (API content generator — write/section/system/preflight), `scripts/lib/keyword_data.py` (keyword demand ladder), `scripts/humanize_intro.py` (not used for API-generated content per _content-generation.md "What this retires"), `scripts/competitor_monitor/scraper.py` (detect/record/show/baseline).
- **Connected SEO data sources**: keyword demand ladder always available (`scripts/lib/keyword_data.py`) — not required for competitor-monitor (relevance gate uses static niche include/exclude terms in competitors.json, not live SEO data).
- **Existing tools/interactives**: `src/pages/calculators/` (calculator routes) + `src/lib/*` (calculator engines) + `src/components/*` (React islands). SoftwareApplication JSON-LD present per CONTENT.md convention. Phase 3c mirrors this pattern for any NET-NEW tool candidate.

Auto-continue to competitor-monitor-specific discovery.
