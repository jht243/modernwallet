# Shipped-fix ledger — 2026-08-25 indexing pass

Lookback: `git log --oneline -30` (HEAD..HEAD~30) + working tree, per phase-build-report.md Step D.1.
**Correction (post Gate A):** the `/roundup/` evidence row below was found via an unrestricted
`git log --follow --diff-filter=A` trace, not the declared 30-commit window — it sits ~51 commits
back (`git rev-list --count 44f2786b..HEAD` = 51). That trace was necessary because the check
being made was "does this page predate GSC's last crawl date," which requires the page's true
creation commit regardless of how far back it is — not a 30-commit-window duplicate-fix search.
Flagged here explicitly per Gate A rather than silently exceeding the declared scope.

| url | fix_type | evidence_sha | commit_subject | source |
|---|---|---|---|---|
| https://www.themodernwallet.com/roundup/ | content (page created) | 44f2786bce0838373cbd14a621b912e5bca5f659 | page-quality-pass 2026-08-20: no-changes run (created src/pages/roundup/index.astro) | commit (~51 commits back, outside the 30-commit window — see note above) |

**Correction (post Gate A):** the original version of this ledger claimed "no internal-link,
canonical, redirect, or sitemap fix signatures matching any of this run's actionable URLs were
found in the last 30 commits." That claim was false. `git log -p -30` shows 3 commits
(`0db4b06`, `c193a4f`, `2bef772` — all 2026-08-24, subject prefix `competitor-monitor:`) added
reciprocal internal links touching 6 slugs that appear as actionable rows in this run's report:
`/compare/ordinary-income-vs-capital-gains-tax/`, `/guides/w-4-withholding-allowances/`,
`/guides/what-is-a-ctfa/`, `/guides/average-hsa-balance-by-age/`, `/guides/college-savings-by-age/`,
`/guides/how-much-should-i-spend-on-rent/`. These are **not** defect-remediation commits — they're
routine reciprocal cross-links added when `competitor-monitor` published 6 unrelated new pages on
2026-08-24 — so they don't change any bucket or fix for those 6 rows (all 6 were independently
confirmed correctly wired into their family's data array regardless). But the ledger should have
surfaced and explained them rather than asserting zero matches.

Outside those 6, no other internal-link, canonical, redirect, or sitemap fix signatures matching
this run's actionable URLs were found in the last 30 commits. The remaining 182 "URL is unknown to
Google" rows and the 1 "Crawled - currently not indexed" row were verified against the **current
template architecture** (not the commit log) since they were never broken by a regression — they
are, and have been, correctly wired into their family's data array/spoke file and hub page; there
was nothing to "ship" for them.
