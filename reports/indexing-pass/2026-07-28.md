---
indexing_pass_date: 2026-07-28
source: GSC URL Inspection API sweep (reports/indexing-pass/2026-07-28.inspection.json)
base_url: https://www.themodernwallet.com
site_url: sc-domain:themodernwallet.com
generated_by: indexing-issues-gsc-pass build phase — .claude/tools/indexing-issues-gsc-pass/gsc_url_inspection.py
status: ready-for-execution
---

## Coverage note

Same sitemap gap as prior sweeps (2026-07-14, 2026-07-21): the tool's hardcoded `/sitemap.xml` probe
404s on this Astro site (real sitemap is `sitemap-index.xml` -> `sitemap-0.xml` via `@astrojs/sitemap`,
per robots.txt), and the tool's naive `sc-domain:` derivation from `www.themodernwallet.com` 403'd (the
SA is granted on the apex `sc-domain:themodernwallet.com` property). Worked around identically to prior
passes: fetched `sitemap-0.xml` directly (540 URLs, up from 504 on 2026-07-21 — 36 net-new URLs published
by other routines since), and swept with `--site-url sc-domain:themodernwallet.com --urls-file`.

Coverage is complete: 540/540 inspected (325 carried forward as still-indexed from the 2026-07-21 baseline,
215 freshly inspected), 0 deferred, not partial, 0 skipped child sitemaps.

Coverage-state breakdown: 345 Submitted and indexed, 193 URL is unknown to Google,
1 Crawled - currently not indexed, 1 Not found (404).

| url | coverage_state | diagnosis | fix | bucket | target |
|---|---|---|---|---|---|
| https://www.themodernwallet.com/auto-loan/refinance-calculator/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /calculators/ hub -> /auto-loan/ category index -> spoke, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/index.astro (auto-loan) |
| https://www.themodernwallet.com/business-loan-payoff/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /calculators/ index (src/pages/calculators/index.astro renders href={`/${c.id}/`} for every calculator category, incl. business-loan-payoff), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/calculators/index.astro |
| https://www.themodernwallet.com/coast-fire/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /calculators/ index (src/pages/calculators/index.astro renders href={`/${c.id}/`} for every calculator category, incl. coast-fire), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/calculators/index.astro |
| https://www.themodernwallet.com/compare/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /compare/ index (src/pages/compare/index.astro loops all comparisons.ts entries and renders href={`/compare/${c.slug}/`}); also cross-linked via footer + relatedComparisons, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/compare/index.astro |
| https://www.themodernwallet.com/compare/15-year-vs-20-year-vs-30-year-mortgage/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /compare/ index (src/pages/compare/index.astro loops all comparisons.ts entries and renders href={`/compare/${c.slug}/`}); also cross-linked via footer + relatedComparisons, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/compare/index.astro |
| https://www.themodernwallet.com/compare/403b-vs-401k/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /compare/ index (src/pages/compare/index.astro loops all comparisons.ts entries and renders href={`/compare/${c.slug}/`}); also cross-linked via footer + relatedComparisons, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/compare/index.astro |
| https://www.themodernwallet.com/compare/457b-vs-401k/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /compare/ index (src/pages/compare/index.astro loops all comparisons.ts entries and renders href={`/compare/${c.slug}/`}); also cross-linked via footer + relatedComparisons, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/compare/index.astro |
| https://www.themodernwallet.com/compare/annuity-vs-cd/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /compare/ index (src/pages/compare/index.astro loops all comparisons.ts entries and renders href={`/compare/${c.slug}/`}); also cross-linked via footer + relatedComparisons, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/compare/index.astro |
| https://www.themodernwallet.com/compare/bilt-rewards-vs-chase-ultimate-rewards/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /compare/ index (src/pages/compare/index.astro loops all comparisons.ts entries and renders href={`/compare/${c.slug}/`}); also cross-linked via footer + relatedComparisons, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/compare/index.astro |
| https://www.themodernwallet.com/compare/business-line-of-credit-vs-sba-loan/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /compare/ index (src/pages/compare/index.astro loops all comparisons.ts entries and renders href={`/compare/${c.slug}/`}); also cross-linked via footer + relatedComparisons, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/compare/index.astro |
| https://www.themodernwallet.com/compare/cash-out-refinance-vs-heloc/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /compare/ index (src/pages/compare/index.astro loops all comparisons.ts entries and renders href={`/compare/${c.slug}/`}); also cross-linked via footer + relatedComparisons, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/compare/index.astro |
| https://www.themodernwallet.com/compare/cd-vs-money-market/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /compare/ index (src/pages/compare/index.astro loops all comparisons.ts entries and renders href={`/compare/${c.slug}/`}); also cross-linked via footer + relatedComparisons, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/compare/index.astro |
| https://www.themodernwallet.com/compare/elder-law-attorney-vs-estate-planning-attorney/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /compare/ index (src/pages/compare/index.astro loops all comparisons.ts entries and renders href={`/compare/${c.slug}/`}); also cross-linked via footer + relatedComparisons, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/compare/index.astro |
| https://www.themodernwallet.com/compare/factor-rate-vs-interest-rate/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /compare/ index (src/pages/compare/index.astro loops all comparisons.ts entries and renders href={`/compare/${c.slug}/`}); also cross-linked via footer + relatedComparisons, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/compare/index.astro |
| https://www.themodernwallet.com/compare/fha-loan-vs-va-loan/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /compare/ index (src/pages/compare/index.astro loops all comparisons.ts entries and renders href={`/compare/${c.slug}/`}); also cross-linked via footer + relatedComparisons, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/compare/index.astro |
| https://www.themodernwallet.com/compare/fha-vs-conventional-loan/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /compare/ index (src/pages/compare/index.astro loops all comparisons.ts entries and renders href={`/compare/${c.slug}/`}); also cross-linked via footer + relatedComparisons, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/compare/index.astro |
| https://www.themodernwallet.com/compare/financial-advisor-vs-cpa/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /compare/ index (src/pages/compare/index.astro loops all comparisons.ts entries and renders href={`/compare/${c.slug}/`}); also cross-linked via footer + relatedComparisons, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/compare/index.astro |
| https://www.themodernwallet.com/compare/financial-advisor-vs-financial-planner/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /compare/ index (src/pages/compare/index.astro loops all comparisons.ts entries and renders href={`/compare/${c.slug}/`}); also cross-linked via footer + relatedComparisons, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/compare/index.astro |
| https://www.themodernwallet.com/compare/financial-advisor-vs-wealth-manager/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /compare/ index (src/pages/compare/index.astro loops all comparisons.ts entries and renders href={`/compare/${c.slug}/`}); also cross-linked via footer + relatedComparisons, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/compare/index.astro |
| https://www.themodernwallet.com/compare/gusto-401k-vs-paychex-401k/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /compare/ index (src/pages/compare/index.astro loops all comparisons.ts entries and renders href={`/compare/${c.slug}/`}); also cross-linked via footer + relatedComparisons, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/compare/index.astro |
| https://www.themodernwallet.com/compare/heloc-vs-home-equity-loan/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /compare/ index (src/pages/compare/index.astro loops all comparisons.ts entries and renders href={`/compare/${c.slug}/`}); also cross-linked via footer + relatedComparisons, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/compare/index.astro |
| https://www.themodernwallet.com/compare/heloc-vs-personal-loan/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /compare/ index (src/pages/compare/index.astro loops all comparisons.ts entries and renders href={`/compare/${c.slug}/`}); also cross-linked via footer + relatedComparisons, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/compare/index.astro |
| https://www.themodernwallet.com/compare/index-fund-vs-etf/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /compare/ index (src/pages/compare/index.astro loops all comparisons.ts entries and renders href={`/compare/${c.slug}/`}); also cross-linked via footer + relatedComparisons, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/compare/index.astro |
| https://www.themodernwallet.com/compare/index-fund-vs-mutual-fund/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /compare/ index (src/pages/compare/index.astro loops all comparisons.ts entries and renders href={`/compare/${c.slug}/`}); also cross-linked via footer + relatedComparisons, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/compare/index.astro |
| https://www.themodernwallet.com/compare/invoice-factoring-vs-invoice-discounting/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /compare/ index (src/pages/compare/index.astro loops all comparisons.ts entries and renders href={`/compare/${c.slug}/`}); also cross-linked via footer + relatedComparisons, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/compare/index.astro |
| https://www.themodernwallet.com/compare/living-trust-vs-lady-bird-deed/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /compare/ index (src/pages/compare/index.astro loops all comparisons.ts entries and renders href={`/compare/${c.slug}/`}); also cross-linked via footer + relatedComparisons, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/compare/index.astro |
| https://www.themodernwallet.com/compare/merchant-cash-advance-vs-revenue-based-financing/ | Crawled - currently not indexed | ✅ shipped in 5bb3a10 (Indexing pass 2026-07-21: added inbound link from /compare/merchant-cash-advance-vs-loan/'s relatedComparisons). Page is not thin (11.5k chars of unique data, 7-row comparison table, 5 FAQs) — already confirmed adequate on the 2026-07-21 pass. That pass also added a new inbound link (relatedComparisons entry in merchant-cash-advance-vs-loan, verified still present at src/data/comparisons-business.ts:83 today). "Crawled – currently not indexed" a week later reads as normal Google evaluation lag for this content, not a defect — no further code change indicated. | No action — fix already shipped in 5bb3a10; recorded so it isn't redone; awaiting Google recrawl (NO lastmod bump, NO re-submit — the page didn't change this run). | already-fixed — awaiting recrawl | N/A — already shipped |
| https://www.themodernwallet.com/compare/military-retirement-vs-medical-retirement/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /compare/ index (src/pages/compare/index.astro loops all comparisons.ts entries and renders href={`/compare/${c.slug}/`}); also cross-linked via footer + relatedComparisons, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/compare/index.astro |
| https://www.themodernwallet.com/compare/military-retirement-vs-va-disability/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /compare/ index (src/pages/compare/index.astro loops all comparisons.ts entries and renders href={`/compare/${c.slug}/`}); also cross-linked via footer + relatedComparisons, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/compare/index.astro |
| https://www.themodernwallet.com/compare/pension-vs-401k/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /compare/ index (src/pages/compare/index.astro loops all comparisons.ts entries and renders href={`/compare/${c.slug}/`}); also cross-linked via footer + relatedComparisons, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/compare/index.astro |
| https://www.themodernwallet.com/compare/power-of-attorney-vs-guardianship/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /compare/ index (src/pages/compare/index.astro loops all comparisons.ts entries and renders href={`/compare/${c.slug}/`}); also cross-linked via footer + relatedComparisons, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/compare/index.astro |
| https://www.themodernwallet.com/compare/rollover-ira-vs-roth-ira/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /compare/ index (src/pages/compare/index.astro loops all comparisons.ts entries and renders href={`/compare/${c.slug}/`}); also cross-linked via footer + relatedComparisons, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/compare/index.astro |
| https://www.themodernwallet.com/compare/rollover-ira-vs-traditional-ira/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /compare/ index (src/pages/compare/index.astro loops all comparisons.ts entries and renders href={`/compare/${c.slug}/`}); also cross-linked via footer + relatedComparisons, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/compare/index.astro |
| https://www.themodernwallet.com/compare/roth-ira-vs-traditional-ira/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /compare/ index (src/pages/compare/index.astro loops all comparisons.ts entries and renders href={`/compare/${c.slug}/`}); also cross-linked via footer + relatedComparisons, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/compare/index.astro |
| https://www.themodernwallet.com/compare/secured-vs-unsecured-loan/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /compare/ index (src/pages/compare/index.astro loops all comparisons.ts entries and renders href={`/compare/${c.slug}/`}); also cross-linked via footer + relatedComparisons, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/compare/index.astro |
| https://www.themodernwallet.com/compare/sep-ira-vs-solo-401k/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /compare/ index (src/pages/compare/index.astro loops all comparisons.ts entries and renders href={`/compare/${c.slug}/`}); also cross-linked via footer + relatedComparisons, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/compare/index.astro |
| https://www.themodernwallet.com/compare/simple-ira-vs-401k/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /compare/ index (src/pages/compare/index.astro loops all comparisons.ts entries and renders href={`/compare/${c.slug}/`}); also cross-linked via footer + relatedComparisons, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/compare/index.astro |
| https://www.themodernwallet.com/compare/stocks-vs-bonds-vs-cash/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /compare/ index (src/pages/compare/index.astro loops all comparisons.ts entries and renders href={`/compare/${c.slug}/`}); also cross-linked via footer + relatedComparisons, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/compare/index.astro |
| https://www.themodernwallet.com/compare/traditional-ira-vs-401k/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /compare/ index (src/pages/compare/index.astro loops all comparisons.ts entries and renders href={`/compare/${c.slug}/`}); also cross-linked via footer + relatedComparisons, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/compare/index.astro |
| https://www.themodernwallet.com/compare/tsp-vs-401k/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /compare/ index (src/pages/compare/index.astro loops all comparisons.ts entries and renders href={`/compare/${c.slug}/`}); also cross-linked via footer + relatedComparisons, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/compare/index.astro |
| https://www.themodernwallet.com/compare/whole-life-vs-term-life-insurance/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /compare/ index (src/pages/compare/index.astro loops all comparisons.ts entries and renders href={`/compare/${c.slug}/`}); also cross-linked via footer + relatedComparisons, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/compare/index.astro |
| https://www.themodernwallet.com/editorial-policy/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via sitewide footer (src/components/SiteFooter.astro) + /about/, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/components/SiteFooter.astro |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/alaska/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via elder-care/medicaid-spend-down-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (elder-care/medicaid-spend-down-calculator) |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/arkansas/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via elder-care/medicaid-spend-down-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (elder-care/medicaid-spend-down-calculator) |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/colorado/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via elder-care/medicaid-spend-down-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (elder-care/medicaid-spend-down-calculator) |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/delaware/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via elder-care/medicaid-spend-down-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (elder-care/medicaid-spend-down-calculator) |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/georgia/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via elder-care/medicaid-spend-down-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (elder-care/medicaid-spend-down-calculator) |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/illinois/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via elder-care/medicaid-spend-down-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (elder-care/medicaid-spend-down-calculator) |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/iowa/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via elder-care/medicaid-spend-down-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (elder-care/medicaid-spend-down-calculator) |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/kansas/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via elder-care/medicaid-spend-down-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (elder-care/medicaid-spend-down-calculator) |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/kentucky/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via elder-care/medicaid-spend-down-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (elder-care/medicaid-spend-down-calculator) |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/maine/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via elder-care/medicaid-spend-down-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (elder-care/medicaid-spend-down-calculator) |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/maryland/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via elder-care/medicaid-spend-down-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (elder-care/medicaid-spend-down-calculator) |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/massachusetts/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via elder-care/medicaid-spend-down-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (elder-care/medicaid-spend-down-calculator) |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/minnesota/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via elder-care/medicaid-spend-down-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (elder-care/medicaid-spend-down-calculator) |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/mississippi/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via elder-care/medicaid-spend-down-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (elder-care/medicaid-spend-down-calculator) |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/missouri/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via elder-care/medicaid-spend-down-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (elder-care/medicaid-spend-down-calculator) |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/montana/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via elder-care/medicaid-spend-down-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (elder-care/medicaid-spend-down-calculator) |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/nevada/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via elder-care/medicaid-spend-down-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (elder-care/medicaid-spend-down-calculator) |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/new-hampshire/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via elder-care/medicaid-spend-down-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (elder-care/medicaid-spend-down-calculator) |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/new-jersey/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via elder-care/medicaid-spend-down-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (elder-care/medicaid-spend-down-calculator) |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/new-mexico/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via elder-care/medicaid-spend-down-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (elder-care/medicaid-spend-down-calculator) |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/new-york/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via elder-care/medicaid-spend-down-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (elder-care/medicaid-spend-down-calculator) |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/north-carolina/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via elder-care/medicaid-spend-down-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (elder-care/medicaid-spend-down-calculator) |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/oklahoma/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via elder-care/medicaid-spend-down-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (elder-care/medicaid-spend-down-calculator) |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/oregon/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via elder-care/medicaid-spend-down-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (elder-care/medicaid-spend-down-calculator) |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/south-carolina/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via elder-care/medicaid-spend-down-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (elder-care/medicaid-spend-down-calculator) |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/tennessee/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via elder-care/medicaid-spend-down-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (elder-care/medicaid-spend-down-calculator) |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/utah/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via elder-care/medicaid-spend-down-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (elder-care/medicaid-spend-down-calculator) |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/vermont/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via elder-care/medicaid-spend-down-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (elder-care/medicaid-spend-down-calculator) |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/virginia/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via elder-care/medicaid-spend-down-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (elder-care/medicaid-spend-down-calculator) |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/washington/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via elder-care/medicaid-spend-down-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (elder-care/medicaid-spend-down-calculator) |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/west-virginia/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via elder-care/medicaid-spend-down-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (elder-care/medicaid-spend-down-calculator) |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/wisconsin/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via elder-care/medicaid-spend-down-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (elder-care/medicaid-spend-down-calculator) |
| https://www.themodernwallet.com/estate-planning/will-cost-calculator/georgia/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via estate-planning/will-cost-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (estate-planning/will-cost-calculator) |
| https://www.themodernwallet.com/estate-planning/will-cost-calculator/kentucky/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via estate-planning/will-cost-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (estate-planning/will-cost-calculator) |
| https://www.themodernwallet.com/estate-planning/will-cost-calculator/mississippi/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via estate-planning/will-cost-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (estate-planning/will-cost-calculator) |
| https://www.themodernwallet.com/estate-planning/will-cost-calculator/montana/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via estate-planning/will-cost-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (estate-planning/will-cost-calculator) |
| https://www.themodernwallet.com/estate-planning/will-cost-calculator/new-hampshire/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via estate-planning/will-cost-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (estate-planning/will-cost-calculator) |
| https://www.themodernwallet.com/estate-planning/will-cost-calculator/new-jersey/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via estate-planning/will-cost-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (estate-planning/will-cost-calculator) |
| https://www.themodernwallet.com/estate-planning/will-cost-calculator/new-york/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via estate-planning/will-cost-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (estate-planning/will-cost-calculator) |
| https://www.themodernwallet.com/estate-planning/will-cost-calculator/north-carolina/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via estate-planning/will-cost-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (estate-planning/will-cost-calculator) |
| https://www.themodernwallet.com/estate-planning/will-cost-calculator/ohio/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via estate-planning/will-cost-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (estate-planning/will-cost-calculator) |
| https://www.themodernwallet.com/estate-planning/will-cost-calculator/rhode-island/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via estate-planning/will-cost-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (estate-planning/will-cost-calculator) |
| https://www.themodernwallet.com/estate-planning/will-cost-calculator/texas/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via estate-planning/will-cost-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (estate-planning/will-cost-calculator) |
| https://www.themodernwallet.com/estate-planning/will-cost-calculator/virginia/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via estate-planning/will-cost-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (estate-planning/will-cost-calculator) |
| https://www.themodernwallet.com/estate-planning/will-cost-calculator/west-virginia/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via estate-planning/will-cost-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (estate-planning/will-cost-calculator) |
| https://www.themodernwallet.com/guides/250th-anniversary-coins/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /guides/ index (src/pages/guides/index.astro loops all guides.ts entries and renders href={`/guides/${g.slug}/`}); also footer-linked, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/guides/index.astro |
| https://www.themodernwallet.com/guides/401k-beneficiary-rules/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /guides/ index (src/pages/guides/index.astro loops all guides.ts entries and renders href={`/guides/${g.slug}/`}); also footer-linked, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/guides/index.astro |
| https://www.themodernwallet.com/guides/ai-financial-advice-chatbots-guide/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /guides/ index (src/pages/guides/index.astro loops all guides.ts entries and renders href={`/guides/${g.slug}/`}); also footer-linked, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/guides/index.astro |
| https://www.themodernwallet.com/guides/bitcoin-retirement-calculator-considerations/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /guides/ index (src/pages/guides/index.astro loops all guides.ts entries and renders href={`/guides/${g.slug}/`}); also footer-linked, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/guides/index.astro |
| https://www.themodernwallet.com/guides/current-mortgage-rates-guide/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /guides/ index (src/pages/guides/index.astro loops all guides.ts entries and renders href={`/guides/${g.slug}/`}); also footer-linked, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/guides/index.astro |
| https://www.themodernwallet.com/guides/financial-advisor-worth-it/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /guides/ index (src/pages/guides/index.astro loops all guides.ts entries and renders href={`/guides/${g.slug}/`}); also footer-linked, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/guides/index.astro |
| https://www.themodernwallet.com/guides/financial-planning-for-30-year-olds/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /guides/ index (src/pages/guides/index.astro loops all guides.ts entries and renders href={`/guides/${g.slug}/`}); also footer-linked, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/guides/index.astro |
| https://www.themodernwallet.com/guides/high-yield-savings-account-for-kids-and-teens/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /guides/ index (src/pages/guides/index.astro loops all guides.ts entries and renders href={`/guides/${g.slug}/`}); also footer-linked, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/guides/index.astro |
| https://www.themodernwallet.com/guides/how-much-to-save-for-retirement-average-earner/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /guides/ index (src/pages/guides/index.astro loops all guides.ts entries and renders href={`/guides/${g.slug}/`}); also footer-linked, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/guides/index.astro |
| https://www.themodernwallet.com/guides/how-to-account-for-invoice-factoring/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /guides/ index (src/pages/guides/index.astro loops all guides.ts entries and renders href={`/guides/${g.slug}/`}); also footer-linked, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/guides/index.astro |
| https://www.themodernwallet.com/guides/how-to-invest-100k-to-1-million/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /guides/ index (src/pages/guides/index.astro loops all guides.ts entries and renders href={`/guides/${g.slug}/`}); also footer-linked, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/guides/index.astro |
| https://www.themodernwallet.com/guides/how-to-invest-200k/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /guides/ index (src/pages/guides/index.astro loops all guides.ts entries and renders href={`/guides/${g.slug}/`}); also footer-linked, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/guides/index.astro |
| https://www.themodernwallet.com/guides/how-to-read-a-personal-loan-review/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /guides/ index (src/pages/guides/index.astro loops all guides.ts entries and renders href={`/guides/${g.slug}/`}); also footer-linked, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/guides/index.astro |
| https://www.themodernwallet.com/guides/how-to-retire-at-40/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /guides/ index (src/pages/guides/index.astro loops all guides.ts entries and renders href={`/guides/${g.slug}/`}); also footer-linked, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/guides/index.astro |
| https://www.themodernwallet.com/guides/how-to-retire-in-10-years-with-no-savings/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /guides/ index (src/pages/guides/index.astro loops all guides.ts entries and renders href={`/guides/${g.slug}/`}); also footer-linked, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/guides/index.astro |
| https://www.themodernwallet.com/guides/international-high-yield-savings-accounts/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /guides/ index (src/pages/guides/index.astro loops all guides.ts entries and renders href={`/guides/${g.slug}/`}); also footer-linked, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/guides/index.astro |
| https://www.themodernwallet.com/guides/international-retirement-calculators/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /guides/ index (src/pages/guides/index.astro loops all guides.ts entries and renders href={`/guides/${g.slug}/`}); also footer-linked, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/guides/index.astro |
| https://www.themodernwallet.com/guides/is-3-million-enough-to-retire-at-40/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /guides/ index (src/pages/guides/index.astro loops all guides.ts entries and renders href={`/guides/${g.slug}/`}); also footer-linked, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/guides/index.astro |
| https://www.themodernwallet.com/guides/is-60-40-portfolio-dead/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /guides/ index (src/pages/guides/index.astro loops all guides.ts entries and renders href={`/guides/${g.slug}/`}); also footer-linked, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/guides/index.astro |
| https://www.themodernwallet.com/guides/is-a-living-trust-worth-it/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /guides/ index (src/pages/guides/index.astro loops all guides.ts entries and renders href={`/guides/${g.slug}/`}); also footer-linked, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/guides/index.astro |
| https://www.themodernwallet.com/guides/is-extended-car-warranty-worth-it/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /guides/ index (src/pages/guides/index.astro loops all guides.ts entries and renders href={`/guides/${g.slug}/`}); also footer-linked, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/guides/index.astro |
| https://www.themodernwallet.com/guides/lending-money-to-family-boundaries/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /guides/ index (src/pages/guides/index.astro loops all guides.ts entries and renders href={`/guides/${g.slug}/`}); also footer-linked, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/guides/index.astro |
| https://www.themodernwallet.com/guides/lessons-from-2008-housing-crash/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /guides/ index (src/pages/guides/index.astro loops all guides.ts entries and renders href={`/guides/${g.slug}/`}); also footer-linked, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/guides/index.astro |
| https://www.themodernwallet.com/guides/retirement-calculator-spreadsheet-template/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /guides/ index (src/pages/guides/index.astro loops all guides.ts entries and renders href={`/guides/${g.slug}/`}); also footer-linked, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/guides/index.astro |
| https://www.themodernwallet.com/guides/sales-tax-holidays-guide/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /guides/ index (src/pages/guides/index.astro loops all guides.ts entries and renders href={`/guides/${g.slug}/`}); also footer-linked, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/guides/index.astro |
| https://www.themodernwallet.com/merchant-cash-advance/merchant-cash-advance-payoff-calculator/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /calculators/ hub -> /merchant-cash-advance/ category index -> spoke, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/index.astro (merchant-cash-advance) |
| https://www.themodernwallet.com/methodology/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via sitewide footer (src/components/SiteFooter.astro) + /about/, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/components/SiteFooter.astro |
| https://www.themodernwallet.com/mortgage/down-payment-calculator/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /calculators/ hub -> /mortgage/ category index -> spoke (src/pages/[category]/index.astro renders href={`/${s.calculator}/${s.slug}/`} for every spoke), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/index.astro (mortgage) |
| https://www.themodernwallet.com/mortgage/piti-calculator/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /calculators/ hub -> /mortgage/ category index -> spoke (src/pages/[category]/index.astro renders href={`/${s.calculator}/${s.slug}/`} for every spoke), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/index.astro (mortgage) |
| https://www.themodernwallet.com/personal-loan/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /calculators/ index (src/pages/calculators/index.astro renders href={`/${c.id}/`} for every calculator category, incl. personal-loan), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/calculators/index.astro |
| https://www.themodernwallet.com/portfolio/70-30-portfolio-calculator/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /calculators/ hub -> /portfolio/ category index -> spoke (src/pages/[category]/index.astro renders href={`/${s.calculator}/${s.slug}/`} for every spoke), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/index.astro (portfolio) |
| https://www.themodernwallet.com/portfolio/80-20-portfolio-calculator/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /calculators/ hub -> /portfolio/ category index -> spoke (src/pages/[category]/index.astro renders href={`/${s.calculator}/${s.slug}/`} for every spoke), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/index.astro (portfolio) |
| https://www.themodernwallet.com/portfolio/asset-allocation-calculator/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /calculators/ hub -> /portfolio/ category index -> spoke (src/pages/[category]/index.astro renders href={`/${s.calculator}/${s.slug}/`} for every spoke), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/index.astro (portfolio) |
| https://www.themodernwallet.com/probate/fee-calculator/alaska/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via probate/fee-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (probate/fee-calculator) |
| https://www.themodernwallet.com/probate/fee-calculator/delaware/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via probate/fee-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (probate/fee-calculator) |
| https://www.themodernwallet.com/probate/fee-calculator/florida/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via probate/fee-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (probate/fee-calculator) |
| https://www.themodernwallet.com/probate/fee-calculator/georgia/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via probate/fee-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (probate/fee-calculator) |
| https://www.themodernwallet.com/probate/fee-calculator/hawaii/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via probate/fee-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (probate/fee-calculator) |
| https://www.themodernwallet.com/probate/fee-calculator/idaho/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via probate/fee-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (probate/fee-calculator) |
| https://www.themodernwallet.com/probate/fee-calculator/illinois/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via probate/fee-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (probate/fee-calculator) |
| https://www.themodernwallet.com/probate/fee-calculator/indiana/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via probate/fee-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (probate/fee-calculator) |
| https://www.themodernwallet.com/probate/fee-calculator/kansas/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via probate/fee-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (probate/fee-calculator) |
| https://www.themodernwallet.com/probate/fee-calculator/kentucky/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via probate/fee-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (probate/fee-calculator) |
| https://www.themodernwallet.com/probate/fee-calculator/louisiana/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via probate/fee-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (probate/fee-calculator) |
| https://www.themodernwallet.com/probate/fee-calculator/maryland/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via probate/fee-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (probate/fee-calculator) |
| https://www.themodernwallet.com/probate/fee-calculator/massachusetts/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via probate/fee-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (probate/fee-calculator) |
| https://www.themodernwallet.com/probate/fee-calculator/michigan/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via probate/fee-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (probate/fee-calculator) |
| https://www.themodernwallet.com/probate/fee-calculator/minnesota/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via probate/fee-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (probate/fee-calculator) |
| https://www.themodernwallet.com/probate/fee-calculator/missouri/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via probate/fee-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (probate/fee-calculator) |
| https://www.themodernwallet.com/probate/fee-calculator/montana/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via probate/fee-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (probate/fee-calculator) |
| https://www.themodernwallet.com/probate/fee-calculator/nebraska/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via probate/fee-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (probate/fee-calculator) |
| https://www.themodernwallet.com/probate/fee-calculator/nevada/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via probate/fee-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (probate/fee-calculator) |
| https://www.themodernwallet.com/probate/fee-calculator/new-hampshire/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via probate/fee-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (probate/fee-calculator) |
| https://www.themodernwallet.com/probate/fee-calculator/new-jersey/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via probate/fee-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (probate/fee-calculator) |
| https://www.themodernwallet.com/probate/fee-calculator/new-york/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via probate/fee-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (probate/fee-calculator) |
| https://www.themodernwallet.com/probate/fee-calculator/north-dakota/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via probate/fee-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (probate/fee-calculator) |
| https://www.themodernwallet.com/probate/fee-calculator/ohio/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via probate/fee-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (probate/fee-calculator) |
| https://www.themodernwallet.com/probate/fee-calculator/oklahoma/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via probate/fee-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (probate/fee-calculator) |
| https://www.themodernwallet.com/probate/fee-calculator/pennsylvania/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via probate/fee-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (probate/fee-calculator) |
| https://www.themodernwallet.com/probate/fee-calculator/tennessee/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via probate/fee-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (probate/fee-calculator) |
| https://www.themodernwallet.com/probate/fee-calculator/texas/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via probate/fee-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (probate/fee-calculator) |
| https://www.themodernwallet.com/probate/fee-calculator/utah/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via probate/fee-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (probate/fee-calculator) |
| https://www.themodernwallet.com/probate/fee-calculator/vermont/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via probate/fee-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (probate/fee-calculator) |
| https://www.themodernwallet.com/probate/fee-calculator/virginia/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via probate/fee-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (probate/fee-calculator) |
| https://www.themodernwallet.com/probate/fee-calculator/washington/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via probate/fee-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (probate/fee-calculator) |
| https://www.themodernwallet.com/probate/fee-calculator/west-virginia/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via probate/fee-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (probate/fee-calculator) |
| https://www.themodernwallet.com/probate/fee-calculator/wisconsin/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via probate/fee-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (probate/fee-calculator) |
| https://www.themodernwallet.com/probate/fee-calculator/wyoming/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via probate/fee-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (probate/fee-calculator) |
| https://www.themodernwallet.com/probate/timeline-calculator/arkansas/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via probate/timeline-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (probate/timeline-calculator) |
| https://www.themodernwallet.com/probate/timeline-calculator/colorado/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via probate/timeline-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (probate/timeline-calculator) |
| https://www.themodernwallet.com/probate/timeline-calculator/connecticut/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via probate/timeline-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (probate/timeline-calculator) |
| https://www.themodernwallet.com/probate/timeline-calculator/delaware/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via probate/timeline-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (probate/timeline-calculator) |
| https://www.themodernwallet.com/probate/timeline-calculator/florida/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via probate/timeline-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (probate/timeline-calculator) |
| https://www.themodernwallet.com/probate/timeline-calculator/hawaii/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via probate/timeline-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (probate/timeline-calculator) |
| https://www.themodernwallet.com/probate/timeline-calculator/idaho/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via probate/timeline-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (probate/timeline-calculator) |
| https://www.themodernwallet.com/probate/timeline-calculator/illinois/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via probate/timeline-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (probate/timeline-calculator) |
| https://www.themodernwallet.com/probate/timeline-calculator/indiana/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via probate/timeline-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (probate/timeline-calculator) |
| https://www.themodernwallet.com/probate/timeline-calculator/iowa/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via probate/timeline-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (probate/timeline-calculator) |
| https://www.themodernwallet.com/probate/timeline-calculator/kansas/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via probate/timeline-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (probate/timeline-calculator) |
| https://www.themodernwallet.com/probate/timeline-calculator/kentucky/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via probate/timeline-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (probate/timeline-calculator) |
| https://www.themodernwallet.com/probate/timeline-calculator/louisiana/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via probate/timeline-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (probate/timeline-calculator) |
| https://www.themodernwallet.com/probate/timeline-calculator/maine/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via probate/timeline-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (probate/timeline-calculator) |
| https://www.themodernwallet.com/probate/timeline-calculator/maryland/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via probate/timeline-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (probate/timeline-calculator) |
| https://www.themodernwallet.com/probate/timeline-calculator/massachusetts/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via probate/timeline-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (probate/timeline-calculator) |
| https://www.themodernwallet.com/probate/timeline-calculator/minnesota/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via probate/timeline-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (probate/timeline-calculator) |
| https://www.themodernwallet.com/probate/timeline-calculator/mississippi/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via probate/timeline-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (probate/timeline-calculator) |
| https://www.themodernwallet.com/probate/timeline-calculator/missouri/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via probate/timeline-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (probate/timeline-calculator) |
| https://www.themodernwallet.com/probate/timeline-calculator/nebraska/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via probate/timeline-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (probate/timeline-calculator) |
| https://www.themodernwallet.com/probate/timeline-calculator/nevada/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via probate/timeline-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (probate/timeline-calculator) |
| https://www.themodernwallet.com/probate/timeline-calculator/new-jersey/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via probate/timeline-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (probate/timeline-calculator) |
| https://www.themodernwallet.com/probate/timeline-calculator/new-york/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via probate/timeline-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (probate/timeline-calculator) |
| https://www.themodernwallet.com/probate/timeline-calculator/north-carolina/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via probate/timeline-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (probate/timeline-calculator) |
| https://www.themodernwallet.com/probate/timeline-calculator/oklahoma/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via probate/timeline-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (probate/timeline-calculator) |
| https://www.themodernwallet.com/probate/timeline-calculator/pennsylvania/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via probate/timeline-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (probate/timeline-calculator) |
| https://www.themodernwallet.com/probate/timeline-calculator/south-carolina/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via probate/timeline-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (probate/timeline-calculator) |
| https://www.themodernwallet.com/probate/timeline-calculator/tennessee/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via probate/timeline-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (probate/timeline-calculator) |
| https://www.themodernwallet.com/probate/timeline-calculator/texas/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via probate/timeline-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (probate/timeline-calculator) |
| https://www.themodernwallet.com/probate/timeline-calculator/washington/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via probate/timeline-calculator hub (src/pages/[category]/[slug].astro renders a state pill link for every entry in the spoke's state list), and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/[slug].astro (probate/timeline-calculator) |
| https://www.themodernwallet.com/retirement/ira-early-withdrawal-calculator/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /calculators/ hub -> /retirement/ category index -> spoke, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/[category]/index.astro (retirement) |
| https://www.themodernwallet.com/roundup/ | Not found (404) | ✅ shipped in 57cf759 (Comparison Content Creator: 4 new pages + 2 enriched (created src/pages/roundup/index.astro, the /roundup/ hub page)). GSC last crawled this URL on 2026-07-16 and got a 404 — at that time /roundup/index.astro did not yet exist as a standalone route. It was created in commit 57cf759 (2026-07-20). Live-verified 200 OK today (2026-07-28). | No action — fix already shipped in 57cf759; recorded so it isn't redone; awaiting Google recrawl (NO lastmod bump, NO re-submit — the page didn't change this run). | already-fixed — awaiting recrawl | N/A — already shipped |
| https://www.themodernwallet.com/roundup/best-brokerage-accounts-for-interest-on-cash/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /roundup/ index (src/pages/roundup/index.astro) + footer links to top roundups, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/roundup/index.astro |
| https://www.themodernwallet.com/roundup/best-brokers-for-treasury-bonds/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /roundup/ index (src/pages/roundup/index.astro) + footer links to top roundups, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/roundup/index.astro |
| https://www.themodernwallet.com/roundup/best-budgeting-apps/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /roundup/ index (src/pages/roundup/index.astro) + footer links to top roundups, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/roundup/index.astro |
| https://www.themodernwallet.com/roundup/best-high-yield-savings-accounts/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /roundup/ index (src/pages/roundup/index.astro) + footer links to top roundups, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/roundup/index.astro |
| https://www.themodernwallet.com/roundup/best-invoice-factoring-companies/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /roundup/ index (src/pages/roundup/index.astro) + footer links to top roundups, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/roundup/index.astro |
| https://www.themodernwallet.com/roundup/best-merchant-cash-advance-companies/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /roundup/ index (src/pages/roundup/index.astro) + footer links to top roundups, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/roundup/index.astro |
| https://www.themodernwallet.com/roundup/best-retirement-calculators/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /roundup/ index (src/pages/roundup/index.astro) + footer links to top roundups, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/roundup/index.astro |
| https://www.themodernwallet.com/roundup/business-line-of-credit-rates-by-lender/ | URL is unknown to Google | New/recent content, no linking defect: verified inbound link exists via /roundup/ index (src/pages/roundup/index.astro) + footer links to top roundups, and the URL is present in sitemap-0.xml (confirmed — this sweep sourced its URL list directly from that sitemap). Not yet crawled/discovered by Google — normal for a site publishing this fast (recent history shows dozens of new pages/day across several automated content routines). No linking defect to fix. | No code change — already linked + already sitemapped. Per Phase 7's re-submit rule, only URLs that changed this run or were newly added to the sitemap this run get re-submitted; this URL did neither, so it is left for natural (re)crawl. | add internal links | src/pages/roundup/index.astro |

## Exclusions (healthy / intentional — not actionable)

- **345 URLs — Submitted and indexed.** Healthy, excluded from the table above (includes 20 URLs newly confirmed indexed this sweep beyond the 325 carried-forward baseline).

## Buckets seen (informational)

- add internal links: 193
- already-fixed — awaiting recrawl: 2

## Re-submission this run

- 0 URLs changed this run (no code edits were made — see rationale in each row's diagnosis/fix). Per Phase 7's re-submit rule (only URLs that changed this run, or were newly added to the sitemap this run, are re-submitted), nothing from today's sweep qualifies for IndexNow / Google Indexing API re-submission.
- The 193 `add internal links` rows and 2 `already-fixed — awaiting recrawl` rows are all already linked + already sitemapped from prior work; they are left for Google's natural (re)crawl.
