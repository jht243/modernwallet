---
indexing_pass_date: 2026-08-04
source: GSC URL Inspection API sweep (reports/indexing-pass/2026-08-04.inspection.json)
base_url: https://www.themodernwallet.com
site_url: sc-domain:themodernwallet.com
generated_by: indexing-issues-gsc-pass build phase — .claude/tools/indexing-issues-gsc-pass/gsc_url_inspection.py
status: ready-for-execution
---

# Indexing pass — 2026-08-04

**Coverage:** inspected 297, carried-forward 0 (first sweep — no prior baseline to skip against), deferred 0, skipped-sitemaps 1 (tool's hardcoded `/sitemap.xml` probe; gap-filled via `--urls-file` against the real `/sitemap-index.xml` → `/sitemap-0.xml`, 297 URLs — see Notes below).

**Note on GSC property resolution:** the tool's `--base-url` → `sc-domain:<netloc>` auto-derivation includes the `www.` host label, which is wrong for a Domain property (Domain properties cover all hosts). Overrode with `--site-url sc-domain:themodernwallet.com`, matching every prior report's `site_url`.

**Gate A (adversarial ledger review) ran once, FAILED on 2 points, was corrected, and both corrections are reflected in this file:**
1. The `/roundup/` row originally cited commit `b05f0e7` as a "route restoration." Gate A found `b05f0e7` is actually the repository's root/initial commit (position 52/52, outside the `HEAD~30` ledger window) — a bulk import, not a targeted fix — and the file has never changed since. Corrected: no code cause identified for the 2026-07-16 404; reclassified from `already-fixed — awaiting recrawl` to `fix redirect / error page`, and moved from excluded-from-resubmit to included.
2. The shipped-ledger only listed 1 commit; Gate A found commit `3cd4f3f` (in-window) shipped real dedicated inbound links to 4 of the `guides/*` rows. Corrected: those 4 rows' diagnosis now cites `3cd4f3f` explicitly (see shipped-ledger.md). Their bucket/action is unchanged (still first-time IndexNow submission — neither was ever submitted per `submission-ledger.json`).

## Coverage-state breakdown (this sweep)

| Coverage state | Count |
|---|---|
| Submitted and indexed | 206 |
| URL is unknown to Google | 89 |
| Crawled - currently not indexed | 1 |
| Not found (404) | 1 |
| **Total inspected** | **297** |

## Actionable rows (91)

| url | coverage_state | diagnosis | fix | bucket | target |
|---|---|---|---|---|---|

| https://www.themodernwallet.com/auto-loan/refinance-calculator/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/spokes-*.ts (entry) + src/pages/calculators/index.astro (spokesForCalc, auto-listed) |
| https://www.themodernwallet.com/compare/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/comparisons.ts or comparisons-business.ts (entry) + src/pages/compare/index.astro (auto-listed) |
| https://www.themodernwallet.com/compare/15-year-vs-20-year-vs-30-year-mortgage/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/comparisons.ts or comparisons-business.ts (entry) + src/pages/compare/index.astro (auto-listed) |
| https://www.themodernwallet.com/compare/403b-vs-401k/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/comparisons.ts or comparisons-business.ts (entry) + src/pages/compare/index.astro (auto-listed) |
| https://www.themodernwallet.com/compare/457b-vs-401k/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/comparisons.ts or comparisons-business.ts (entry) + src/pages/compare/index.astro (auto-listed) |
| https://www.themodernwallet.com/compare/457b-vs-403b/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/comparisons.ts or comparisons-business.ts (entry) + src/pages/compare/index.astro (auto-listed) |
| https://www.themodernwallet.com/compare/457b-vs-roth-ira/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/comparisons.ts or comparisons-business.ts (entry) + src/pages/compare/index.astro (auto-listed) |
| https://www.themodernwallet.com/compare/annuity-vs-cd/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/comparisons.ts or comparisons-business.ts (entry) + src/pages/compare/index.astro (auto-listed) |
| https://www.themodernwallet.com/compare/business-line-of-credit-vs-sba-loan/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/comparisons.ts or comparisons-business.ts (entry) + src/pages/compare/index.astro (auto-listed) |
| https://www.themodernwallet.com/compare/cash-out-refinance-vs-heloc/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/comparisons.ts or comparisons-business.ts (entry) + src/pages/compare/index.astro (auto-listed) |
| https://www.themodernwallet.com/compare/cd-vs-money-market/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/comparisons.ts or comparisons-business.ts (entry) + src/pages/compare/index.astro (auto-listed) |
| https://www.themodernwallet.com/compare/elder-law-attorney-vs-estate-planning-attorney/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/comparisons.ts or comparisons-business.ts (entry) + src/pages/compare/index.astro (auto-listed) |
| https://www.themodernwallet.com/compare/factor-rate-vs-interest-rate/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/comparisons.ts or comparisons-business.ts (entry) + src/pages/compare/index.astro (auto-listed) |
| https://www.themodernwallet.com/compare/fha-loan-vs-va-loan/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/comparisons.ts or comparisons-business.ts (entry) + src/pages/compare/index.astro (auto-listed) |
| https://www.themodernwallet.com/compare/fha-vs-conventional-loan/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/comparisons.ts or comparisons-business.ts (entry) + src/pages/compare/index.astro (auto-listed) |
| https://www.themodernwallet.com/compare/financial-advisor-vs-cpa/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/comparisons.ts or comparisons-business.ts (entry) + src/pages/compare/index.astro (auto-listed) |
| https://www.themodernwallet.com/compare/financial-advisor-vs-financial-planner/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/comparisons.ts or comparisons-business.ts (entry) + src/pages/compare/index.astro (auto-listed) |
| https://www.themodernwallet.com/compare/financial-advisor-vs-wealth-manager/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/comparisons.ts or comparisons-business.ts (entry) + src/pages/compare/index.astro (auto-listed) |
| https://www.themodernwallet.com/compare/gusto-401k-vs-paychex-401k/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/comparisons.ts or comparisons-business.ts (entry) + src/pages/compare/index.astro (auto-listed) |
| https://www.themodernwallet.com/compare/heloc-vs-home-equity-loan/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/comparisons.ts or comparisons-business.ts (entry) + src/pages/compare/index.astro (auto-listed) |
| https://www.themodernwallet.com/compare/heloc-vs-personal-loan/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/comparisons.ts or comparisons-business.ts (entry) + src/pages/compare/index.astro (auto-listed) |
| https://www.themodernwallet.com/compare/i-bonds-vs-ee-bonds/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/comparisons.ts or comparisons-business.ts (entry) + src/pages/compare/index.astro (auto-listed) |
| https://www.themodernwallet.com/compare/i-bonds-vs-tips/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/comparisons.ts or comparisons-business.ts (entry) + src/pages/compare/index.astro (auto-listed) |
| https://www.themodernwallet.com/compare/index-fund-vs-etf/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/comparisons.ts or comparisons-business.ts (entry) + src/pages/compare/index.astro (auto-listed) |
| https://www.themodernwallet.com/compare/index-fund-vs-mutual-fund/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/comparisons.ts or comparisons-business.ts (entry) + src/pages/compare/index.astro (auto-listed) |
| https://www.themodernwallet.com/compare/invoice-factoring-vs-invoice-discounting/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/comparisons.ts or comparisons-business.ts (entry) + src/pages/compare/index.astro (auto-listed) |
| https://www.themodernwallet.com/compare/living-trust-vs-lady-bird-deed/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/comparisons.ts or comparisons-business.ts (entry) + src/pages/compare/index.astro (auto-listed) |
| https://www.themodernwallet.com/compare/pension-vs-401k/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/comparisons.ts or comparisons-business.ts (entry) + src/pages/compare/index.astro (auto-listed) |
| https://www.themodernwallet.com/compare/power-of-attorney-vs-guardianship/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/comparisons.ts or comparisons-business.ts (entry) + src/pages/compare/index.astro (auto-listed) |
| https://www.themodernwallet.com/compare/rollover-ira-vs-roth-ira/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/comparisons.ts or comparisons-business.ts (entry) + src/pages/compare/index.astro (auto-listed) |
| https://www.themodernwallet.com/compare/rollover-ira-vs-traditional-ira/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/comparisons.ts or comparisons-business.ts (entry) + src/pages/compare/index.astro (auto-listed) |
| https://www.themodernwallet.com/compare/roth-ira-vs-traditional-ira/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/comparisons.ts or comparisons-business.ts (entry) + src/pages/compare/index.astro (auto-listed) |
| https://www.themodernwallet.com/compare/secured-vs-unsecured-loan/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/comparisons.ts or comparisons-business.ts (entry) + src/pages/compare/index.astro (auto-listed) |
| https://www.themodernwallet.com/compare/sep-ira-vs-solo-401k/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/comparisons.ts or comparisons-business.ts (entry) + src/pages/compare/index.astro (auto-listed) |
| https://www.themodernwallet.com/compare/simple-ira-vs-401k/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/comparisons.ts or comparisons-business.ts (entry) + src/pages/compare/index.astro (auto-listed) |
| https://www.themodernwallet.com/compare/stocks-vs-bonds-vs-cash/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/comparisons.ts or comparisons-business.ts (entry) + src/pages/compare/index.astro (auto-listed) |
| https://www.themodernwallet.com/compare/traditional-ira-vs-401k/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/comparisons.ts or comparisons-business.ts (entry) + src/pages/compare/index.astro (auto-listed) |
| https://www.themodernwallet.com/compare/tsp-vs-401k/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/comparisons.ts or comparisons-business.ts (entry) + src/pages/compare/index.astro (auto-listed) |
| https://www.themodernwallet.com/compare/whole-life-vs-term-life-insurance/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/comparisons.ts or comparisons-business.ts (entry) + src/pages/compare/index.astro (auto-listed) |
| https://www.themodernwallet.com/guides/250th-anniversary-coins/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/guides.ts (entry) + src/pages/guides/index.astro (auto-listed) |
| https://www.themodernwallet.com/guides/401k-beneficiary-rules/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/guides.ts (entry) + src/pages/guides/index.astro (auto-listed) |
| https://www.themodernwallet.com/guides/bitcoin-retirement-calculator-considerations/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/guides.ts (entry) + src/pages/guides/index.astro (auto-listed) |
| https://www.themodernwallet.com/guides/borrowing-against-life-insurance/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/guides.ts (entry) + src/pages/guides/index.astro (auto-listed) |
| https://www.themodernwallet.com/guides/credit-card-gamification/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/guides.ts (entry) + src/pages/guides/index.astro (auto-listed) |
| https://www.themodernwallet.com/guides/financial-advisor-worth-it/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/guides.ts (entry) + src/pages/guides/index.astro (auto-listed) |
| https://www.themodernwallet.com/guides/financial-planning-for-30-year-olds/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/guides.ts (entry) + src/pages/guides/index.astro (auto-listed) |
| https://www.themodernwallet.com/guides/high-yield-savings-account-for-kids-and-teens/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/guides.ts (entry) + src/pages/guides/index.astro (auto-listed) |
| https://www.themodernwallet.com/guides/how-much-does-investing-1000-a-month-grow-to/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): commit 3cd4f3f (in-window) already added a dedicated inbound link from guides/how-to-invest-200k/ to this URL, on top of the standing /guides/ hub auto-listing. Never submitted (not in submission-ledger.json). | No further code change (link already added in 3cd4f3f) — first-time IndexNow re-submit (not suppressed). | add internal links | src/data/guides.ts (entry) + src/pages/guides/index.astro (auto-listed) |
| https://www.themodernwallet.com/guides/how-much-to-save-for-retirement-average-earner/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/guides.ts (entry) + src/pages/guides/index.astro (auto-listed) |
| https://www.themodernwallet.com/guides/how-to-become-a-401k-millionaire/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): commit 3cd4f3f (in-window) already added a dedicated inbound link from guides/how-to-invest-100k-to-1-million/ to this URL, on top of the standing /guides/ hub auto-listing. Never submitted (not in submission-ledger.json). | No further code change (link already added in 3cd4f3f) — first-time IndexNow re-submit (not suppressed). | add internal links | src/data/guides.ts (entry) + src/pages/guides/index.astro (auto-listed) |
| https://www.themodernwallet.com/guides/how-to-budget-when-prices-keep-rising/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/guides.ts (entry) + src/pages/guides/index.astro (auto-listed) |
| https://www.themodernwallet.com/guides/how-to-budget-with-irregular-income/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/guides.ts (entry) + src/pages/guides/index.astro (auto-listed) |
| https://www.themodernwallet.com/guides/how-to-build-a-dividend-portfolio/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/guides.ts (entry) + src/pages/guides/index.astro (auto-listed) |
| https://www.themodernwallet.com/guides/how-to-invest-100k-to-1-million/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/guides.ts (entry) + src/pages/guides/index.astro (auto-listed) |
| https://www.themodernwallet.com/guides/how-to-invest-200k/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/guides.ts (entry) + src/pages/guides/index.astro (auto-listed) |
| https://www.themodernwallet.com/guides/how-to-read-a-personal-loan-review/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/guides.ts (entry) + src/pages/guides/index.astro (auto-listed) |
| https://www.themodernwallet.com/guides/how-to-retire-at-40/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/guides.ts (entry) + src/pages/guides/index.astro (auto-listed) |
| https://www.themodernwallet.com/guides/how-to-retire-at-67/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): commit 3cd4f3f (in-window) already added a dedicated inbound link from guides/how-to-retire-at-40/ to this URL, on top of the standing /guides/ hub auto-listing. Never submitted (not in submission-ledger.json). | No further code change (link already added in 3cd4f3f) — first-time IndexNow re-submit (not suppressed). | add internal links | src/data/guides.ts (entry) + src/pages/guides/index.astro (auto-listed) |
| https://www.themodernwallet.com/guides/how-to-retire-in-10-years-with-no-savings/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/guides.ts (entry) + src/pages/guides/index.astro (auto-listed) |
| https://www.themodernwallet.com/guides/international-high-yield-savings-accounts/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/guides.ts (entry) + src/pages/guides/index.astro (auto-listed) |
| https://www.themodernwallet.com/guides/international-retirement-calculators/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/guides.ts (entry) + src/pages/guides/index.astro (auto-listed) |
| https://www.themodernwallet.com/guides/is-3-million-enough-to-retire-at-40/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/guides.ts (entry) + src/pages/guides/index.astro (auto-listed) |
| https://www.themodernwallet.com/guides/is-60-40-portfolio-dead/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/guides.ts (entry) + src/pages/guides/index.astro (auto-listed) |
| https://www.themodernwallet.com/guides/lending-money-to-family-boundaries/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/guides.ts (entry) + src/pages/guides/index.astro (auto-listed) |
| https://www.themodernwallet.com/guides/mortgage-refinance-lessons-learned/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): commit 3cd4f3f (in-window) already added a dedicated inbound link from guides/current-mortgage-rates-guide/ to this URL, on top of the standing /guides/ hub auto-listing. Never submitted (not in submission-ledger.json). | No further code change (link already added in 3cd4f3f) — first-time IndexNow re-submit (not suppressed). | add internal links | src/data/guides.ts (entry) + src/pages/guides/index.astro (auto-listed) |
| https://www.themodernwallet.com/guides/roth-conversion-ladder/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/guides.ts (entry) + src/pages/guides/index.astro (auto-listed) |
| https://www.themodernwallet.com/guides/roth-conversion-rules/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/guides.ts (entry) + src/pages/guides/index.astro (auto-listed) |
| https://www.themodernwallet.com/guides/what-is-a-good-expense-ratio/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/guides.ts (entry) + src/pages/guides/index.astro (auto-listed) |
| https://www.themodernwallet.com/guides/what-target-date-fund-should-i-choose/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/guides.ts (entry) + src/pages/guides/index.astro (auto-listed) |
| https://www.themodernwallet.com/interest-per-day/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/calculators.ts (top-level entry) + src/pages/calculators/index.astro (auto-listed) |
| https://www.themodernwallet.com/mortgage/down-payment-calculator/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/spokes-*.ts (entry) + src/pages/calculators/index.astro (spokesForCalc, auto-listed) |
| https://www.themodernwallet.com/mortgage/piti-calculator/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/spokes-*.ts (entry) + src/pages/calculators/index.astro (spokesForCalc, auto-listed) |
| https://www.themodernwallet.com/portfolio/70-30-portfolio-calculator/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/spokes-*.ts (entry) + src/pages/calculators/index.astro (spokesForCalc, auto-listed) |
| https://www.themodernwallet.com/portfolio/80-20-portfolio-calculator/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/spokes-*.ts (entry) + src/pages/calculators/index.astro (spokesForCalc, auto-listed) |
| https://www.themodernwallet.com/portfolio/asset-allocation-calculator/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/spokes-*.ts (entry) + src/pages/calculators/index.astro (spokesForCalc, auto-listed) |
| https://www.themodernwallet.com/retirement/ira-early-withdrawal-calculator/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/spokes-*.ts (entry) + src/pages/calculators/index.astro (spokesForCalc, auto-listed) |
| https://www.themodernwallet.com/roundup/best-brokerage-accounts-for-interest-on-cash/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/roundups.ts or roundups-business.ts (entry) + src/pages/roundup/index.astro (auto-listed) |
| https://www.themodernwallet.com/roundup/best-brokers-for-treasury-bonds/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/roundups.ts or roundups-business.ts (entry) + src/pages/roundup/index.astro (auto-listed) |
| https://www.themodernwallet.com/roundup/best-budgeting-apps/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/roundups.ts or roundups-business.ts (entry) + src/pages/roundup/index.astro (auto-listed) |
| https://www.themodernwallet.com/roundup/best-dividend-etfs-for-retirement/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/roundups.ts or roundups-business.ts (entry) + src/pages/roundup/index.astro (auto-listed) |
| https://www.themodernwallet.com/roundup/best-dividend-etfs/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/roundups.ts or roundups-business.ts (entry) + src/pages/roundup/index.astro (auto-listed) |
| https://www.themodernwallet.com/roundup/best-high-yield-savings-accounts/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/roundups.ts or roundups-business.ts (entry) + src/pages/roundup/index.astro (auto-listed) |
| https://www.themodernwallet.com/roundup/best-invoice-factoring-companies/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/roundups.ts or roundups-business.ts (entry) + src/pages/roundup/index.astro (auto-listed) |
| https://www.themodernwallet.com/roundup/best-merchant-cash-advance-companies/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/roundups.ts or roundups-business.ts (entry) + src/pages/roundup/index.astro (auto-listed) |
| https://www.themodernwallet.com/roundup/best-monthly-dividend-etfs/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/roundups.ts or roundups-business.ts (entry) + src/pages/roundup/index.astro (auto-listed) |
| https://www.themodernwallet.com/roundup/best-retirement-calculators/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/roundups.ts or roundups-business.ts (entry) + src/pages/roundup/index.astro (auto-listed) |
| https://www.themodernwallet.com/roundup/best-robo-advisors/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/roundups.ts or roundups-business.ts (entry) + src/pages/roundup/index.astro (auto-listed) |
| https://www.themodernwallet.com/roundup/best-target-date-funds/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/roundups.ts or roundups-business.ts (entry) + src/pages/roundup/index.astro (auto-listed) |
| https://www.themodernwallet.com/roundup/business-line-of-credit-rates-by-lender/ | URL is unknown to Google | Checked ledger (HEAD..HEAD~30): no per-URL fix needed. Page already linked via the site's data-driven hub/spoke template (see shipped-ledger.md diagnosis-method note) — verified for this URL family. Never submitted (not in submission-ledger.json). Root cause is crawl-discovery lag, not a linking defect. | No code change — re-submit via IndexNow to prompt discovery (first submission, not suppressed). | add internal links | src/data/roundups.ts or roundups-business.ts (entry) + src/pages/roundup/index.astro (auto-listed) |
| https://www.themodernwallet.com/compare/merchant-cash-advance-vs-revenue-based-financing/ | Crawled - currently not indexed | Content reviewed against site standard (4 sections + 5 FAQs + sources + comparison table, matches other comparison pages); not thin. Has 2 inbound links already (compare/ hub index + relatedComparisons cross-link from invoice-factoring-vs-business-line-of-credit). Already submitted via IndexNow on 2026-07-21 (14 days ago, within the 30-day suppression window) per submission-ledger.json. | No content/code change — page meets quality bar and is already linked + already submitted. Suppressed from this run's resubmit list (ledger <30 days); let the existing submission take effect. | already-fixed — awaiting recrawl | src/data/comparisons.ts or comparisons-business.ts (entry) + src/pages/compare/index.astro (auto-listed) |
| https://www.themodernwallet.com/roundup/ | Not found (404) | Gate A correction: b05f0e7 is the repo's root/initial commit (position 52/52 in full history, outside the HEAD~30 ledger window) that bulk-added the whole file tree — it is not a targeted fix, and src/pages/roundup/index.astro has never changed since. No code defect found for the 2026-07-16 404. Page verified live at 200 via `curl -I` today; GSC's coverage_state reflects a stale/transient crawl-time result. | No code change (none identified). Treat as a stale GSC record — include in resubmit to prompt a fresh crawl. | fix redirect / error page | src/data/roundups.ts or roundups-business.ts (entry) + src/pages/roundup/index.astro (auto-listed) |


## Exclusions (206 healthy, not shown as rows)

All 206 `Submitted and indexed` URLs are excluded per Step E — healthy, no action needed.

## Notes

- **Sitemap gap-fill:** `.claude/tools/indexing-issues-gsc-pass/gsc_url_inspection.py` hardcodes a probe of `<base>/sitemap.xml`, which 404s on this Astro `@astrojs/sitemap` site (real sitemap is `/sitemap-index.xml` → `/sitemap-0.xml`). Per `phase-build-report.md` Step B.4, fetched the live sitemap-0.xml (297 URLs), wrote them to a file, and ran the tool with `--urls-file --resume` to gap-fill into the same dated JSON (merges, does not overwrite). Final `total: 297` matches the unique sitemap URL count exactly.
- **Bucket "add internal links" (89 rows):** none required a new code change (except the 4 guide rows noted above, which already received one in 3cd4f3f). See `2026-08-04.shipped-ledger.md` for the diagnosis method — every one of these URLs is already linked from a footer-reachable hub page via the site's data-driven template architecture (compare/guides/roundup index auto-listing, or the calculators hub's `spokesForCalc()`/top-level loop, plus `relatedSlugs`/`relatedComparisons` cross-links). Gate A independently re-verified 10 of these 89 against the actual template/data-file source and found no false negatives. Root cause is crawl-discovery lag on recently-shipped pages, not a linking defect. Fix = IndexNow re-submission only.
- **`/roundup/` (404 → `fix redirect / error page`):** see the Gate A correction note above. No code defect found; verified live via `curl -I` → `200`. Included in resubmit to prompt a fresh crawl and correct Google's stale record.
- **`/compare/merchant-cash-advance-vs-revenue-based-financing/` (Crawled - not indexed):** content reviewed, not thin, already linked, already IndexNow-submitted 2026-07-21 (14 days ago) per `submission-ledger.json` — suppressed from this run's resubmit list under the 30-day rule. This is the only row excluded from resubmit.

## Resubmit candidates: 90 of 91

All actionable rows except the ledger-suppressed `merchant-cash-advance-vs-revenue-based-financing` row. See `2026-08-04.resubmit.txt` (written in Phase 7).
