# Manual Indexing Submission Run — 2026-07-11
Account: jonath10@gmail.com (authuser 2 → /u/2/) · Property: sc-domain:themodernwallet.com (Domain property)
Tool: GSC URL Inspection → Request Indexing (manual, via Chrome)

## Outcome: SUCCESS — 10 URLs submitted, quota NOT exceeded
- All 10 non-indexed URLs successfully submitted to Google's priority crawl queue
  (each returned "URL was added to a priority crawl queue. Submitting a page multiple
  times will not change its queue position or priority").
- Daily quota was never hit — a clean 10/10 (better than 07-10, which blocked at 8).
  The "wait 10 min & retry" rule was not triggered (no quota block on the initial
  attempt or during the run).
- Account/property access confirmed via /u/2/ (avatar "J", property picker shows
  themodernwallet.com Domain property) before every submission.

## Prioritization (per scheduled-task instructions)
Task prioritized any page with **Trump / 529 / kids** first, then the most important
pages (homepage, pillar pages, tools pages).

**MAJOR CHANGE SINCE PRIOR RUNS: the site is now ~fully indexed.** Google ran a large
crawl between 06/29 and 07/11. Live URL Inspection ("URL is on Google / Page is indexed")
confirmed that essentially every canonical, sitemap-listed page is now indexed, including:
- Every Trump / 529 / kids / custodial page checked (trump-account-worth-it,
  529-savings-calculator, 529-vs-roth-ira, 529-leftover-money-options,
  best-investment-account-for-kids — ALL already indexed).
- Every top-level hub/pillar checked (tax-resolution/, merchant-cash-advance/,
  invoice-factoring/, business-line-of-credit/, auto-loan/, estate-planning/,
  elder-care/, probate/ — ALL already indexed, with trailing slash).
- The business-financing carryover pages flagged "unknown to Google" on 07-07/07-08
  (small-business-financing-guide, etc. — now indexed).
- The GSC "Crawled – currently not indexed" list (stale, dated 6/29) — all 6 of its
  calculator pages are now indexed (budget/50-30-20-budget-calculator,
  investing/dollar-cost-averaging-calculator, etc.).

So there were **no non-indexed Trump/529/kids pages left to submit.** The run fell back
to the task's secondary priority (pillar + tools pages) and specifically to the ONE pool
of genuinely non-indexed pages still on the site: the programmatic
**tax-resolution / offer-in-compromise-calculator / [state]** tool cluster, which Google
has only partially discovered (some states indexed, many still "URL is unknown to Google").
These are tool/calculator pages, matching the task's "tools pages" priority.

Every URL below was inspected live and confirmed "URL is unknown to Google" (or, for
/budget, likewise unknown) BEFORE Request Indexing. Already-indexed pages were skipped —
no quota was wasted re-submitting indexed URLs.

## Submitted — added to Google's priority crawl queue (all ✅ "Indexing requested")
1.  https://www.themodernwallet.com/budget                                                   ✅ (pillar hub — carryover; was quota-blocked 07-10; "unknown to Google")
2.  https://www.themodernwallet.com/tax-resolution/offer-in-compromise-calculator/wyoming/       ✅ (tool)
3.  https://www.themodernwallet.com/tax-resolution/offer-in-compromise-calculator/vermont/       ✅ (tool)
4.  https://www.themodernwallet.com/tax-resolution/offer-in-compromise-calculator/north-dakota/  ✅ (tool)
5.  https://www.themodernwallet.com/tax-resolution/offer-in-compromise-calculator/montana/       ✅ (tool)
6.  https://www.themodernwallet.com/tax-resolution/offer-in-compromise-calculator/west-virginia/ ✅ (tool)
7.  https://www.themodernwallet.com/tax-resolution/offer-in-compromise-calculator/rhode-island/  ✅ (tool)
8.  https://www.themodernwallet.com/tax-resolution/offer-in-compromise-calculator/new-hampshire/ ✅ (tool)
9.  https://www.themodernwallet.com/tax-resolution/offer-in-compromise-calculator/hawaii/        ✅ (tool)
10. https://www.themodernwallet.com/tax-resolution/offer-in-compromise-calculator/maine/         ✅ (tool)

## Inspected but SKIPPED — already indexed ("URL is on Google / Page is indexed")
Trump / 529 / kids (all indexed — Google has fully caught up on the priority cluster):
- /guides/trump-account-worth-it/
- /529-savings-calculator/
- /compare/529-vs-roth-ira/
- /guides/529-leftover-money-options/
- /guides/best-investment-account-for-kids/
Hubs / tools / pillars (all indexed):
- /tax-resolution/ , /merchant-cash-advance/ , /invoice-factoring/ , /business-line-of-credit/
- /auto-loan/ , /estate-planning/ , /elder-care/ , /probate/
- /guides/small-business-financing-guide/
- /tax-resolution/offer-in-compromise-calculator/ (main, non-state)
- /tax-resolution/penalty-abatement-calculator/
- /budget/50-30-20-budget-calculator/ , /investing/dollar-cost-averaging-calculator/
State OIC pages already indexed (not re-submitted): california, south-dakota, delaware, alaska

## Notable
- **Positive milestone: near-complete indexation.** GSC Overview showed "72 indexed / 9 not
  indexed" but that data is stale (Last update 6/29). Live inspection shows the real picture
  is far better — the great majority of the 448 sitemap URLs are now indexed. The recurring
  5-run "sitemap not associated / hubs unknown to Google" problem has largely resolved: the
  hub/pillar pages submitted on 07-10 are now indexed, and Google crawled deep programmatic
  pages (e.g. state calculators) on its own.
- The remaining non-indexed pool is the **partially-discovered programmatic clusters** — the
  state offer-in-compromise calculators being the clearest example (roughly half still
  "unknown to Google"). Every submitted URL's Discovery panel again showed "No referring
  sitemaps detected" / "Referring page: None detected," i.e. these deep pages have no internal
  links pointing to them, which is why Google finds them slowly. RECOMMEND an internal-linking
  pass: link the state OIC pages from the /tax-resolution/ hub and the main OIC calculator so
  the rest of the cluster gets discovered without needing manual submission.
- URL #1 (/budget) is the no-trailing-slash variant; it serves HTTP 200 and was genuinely
  "unknown to Google" (the sitemap lists /budget/ WITH a slash, which is indexed). Harmless to
  submit, but going forward prefer the exact canonical trailing-slash sitemap URL.
- Two transient MCP Chrome extension disconnects occurred mid-run (during the vermont inspect
  and the maine Request-Indexing click). Both recovered on reconnect; re-fetching tab context
  returned the correct themodernwallet.com /u/2/ tab, and on-screen verification confirmed both
  vermont and maine were submitted successfully. No wrong-URL / wrong-property submission.
- Only sitemap-index.xml → sitemap-0.xml exists (448 URLs, no lastmod dates, no secondary
  sitemap). No newer/undiscovered URL pool beyond the programmatic clusters.

## Priority list for the NEXT run
All Trump/529/kids and all hubs are indexed, so continue with the still-undiscovered
programmatic tool clusters (inspect each first — some get picked up organically):
- Remaining non-indexed state OIC calculators, e.g.:
  - /tax-resolution/offer-in-compromise-calculator/oregon/
  - /tax-resolution/offer-in-compromise-calculator/nevada/
  - /tax-resolution/offer-in-compromise-calculator/kansas/
  - /tax-resolution/offer-in-compromise-calculator/nebraska/
  - /tax-resolution/offer-in-compromise-calculator/iowa/
  - /tax-resolution/offer-in-compromise-calculator/new-mexico/
  - (sweep the full 50-state set; ~half are still "unknown to Google")
- Re-scan the live sitemap for any NEW Trump/529/kids pages published since 07-11 and
  prioritize those ahead of the state calculators.
- Better long-term fix than nightly manual submission: internal-linking pass so the
  /tax-resolution/ hub links to all 50 state calculators (kills the discovery bottleneck).
