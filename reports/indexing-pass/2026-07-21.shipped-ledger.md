# Shipped-fix ledger — 2026-07-21

Lookback: `git log --oneline -50` on branch `claude/weekly-indexing-0nn6zv` — the **entire**
repository history (only 50 commits exist total). Widened past the default 30-commit window
specifically because the single most relevant precedent, `5783b81` (2026-07-14's internal-linking
fix), sits at position 39/50 — outside a default-30 lookback but still the exact same defect class
as 146 of today's 179 actionable rows. Fix signatures searched: internal-link adds (new
`href`/`relatedComparisons`/hub-listing entries), sitemap adds, canonical/redirect changes,
substantive content adds — matched against the 179 actionable URLs in
`2026-07-21.inspection.json`, then **independently re-verified** by building the site
(`npm run build`) and parsing every emitted `dist/**/index.html` for real `href` targets (0
orphans among all 179 — every URL has a genuine live inbound link today).

| url pattern | fix_type | evidence_sha | commit_subject | source |
|---|---|---|---|---|
| 113 state-variant sub-pages under `/probate/fee-calculator/{state}/`, `/probate/timeline-calculator/{state}/`, `/elder-care/medicaid-spend-down-calculator/{state}/`, `/estate-planning/will-cost-calculator/{state}/` | internal link ("Browse {keyword} by state" section) | `5783b81` | indexing-pass 2026-07-14: root-cause + fix internal-linking gaps for 196 non-indexed URLs | commit (`src/pages/[category]/[slug].astro` — added a `stateLinks` section rendering a link to every `entry.stateVariants` sub-page; confirmed present at src/pages/[category]/[slug].astro:176-180 today) |
| 25 `/compare/*` comparison pages | internal link (compare index + relatedComparisons/footer) | `5783b81` | indexing-pass 2026-07-14: root-cause + fix internal-linking gaps for 196 non-indexed URLs | commit (`src/pages/compare/index.astro` created, lists all comparisons; sitewide footer link added; confirmed present today via build-time inbound-link audit) |
| 3 roundup pages (`best-budgeting-apps`, `best-invoice-factoring-companies`, `best-merchant-cash-advance-companies`) | internal link (contextual hub cross-link) | `5783b81` | indexing-pass 2026-07-14: root-cause + fix internal-linking gaps for 196 non-indexed URLs | commit (natural contextual link added from each matching category hub; confirmed present today) |
| `/portfolio/asset-allocation-calculator/` | internal link (calculatorLinks retarget) | `edbdb6d` | ga4-top-pages-pass 2026-07-13: 5 new pages, 4 enrichments, 2 metadata across 9 audited winners | commit (`src/data/comparisons.ts` — `calculatorLinks` retargeted from `/compare/stocks-vs-bonds/` and its sibling to this page; explicitly called out as already-fixed in the 2026-07-14 indexing-pass commit message; confirmed present today) |
| `/roundup/` | fix redirect / error page (404 → 200) | `0e855bd` | fix(seo): add /roundup/ hub + footer link to eliminate orphan pages | commit (`src/pages/roundup/index.astro` created — this route previously had no index page and 404'd; Google's last crawl, 2026-07-16T03:18:44Z, predates this 2026-07-18 fix; verified live 200 today via `curl`) |

## Not in the ledger — genuinely new content, never had a defect (31 rows)

The remaining 31 actionable rows (10 `/guides/*`, 5 `/compliance/*`, 7 newer `/compare/*` pages,
2 `/portfolio/*` calculators, 1 `/roundup/*` page, 4 standalone calculators, `/compare/` index,
`/personal-loan/`) are pages published within the last 1–6 days that were built **with** a genuine
inbound link from day one (confirmed via the same build-time inbound-link audit). They were never
broken, so there is no "fix" to ledger — they simply haven't been discovered/crawled by Google yet.
Report table `fix` column: no code change, re-submit only.

## Verification method (both directions)

- **False-negative check** (did we miss a real prior fix?): cross-referenced today's 179 actionable
  URLs against `reports/indexing-pass/2026-07-14.urls.csv`'s actionable set (197 rows) — 146 exact
  matches found and ledgered above; the 33 non-matches were checked individually against
  `2026-07-14`'s commit message and the current codebase.
- **False-positive check** (did we wrongly drop a row as already-fixed?): every row ledgered above
  was independently re-verified live — either via the build-time `dist/**/index.html` href audit
  (internal links) or a direct `curl` status check (`/roundup/` 404→200). No row was dropped on a
  weak or missing match.
