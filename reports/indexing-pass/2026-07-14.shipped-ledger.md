# Shipped-fix ledger — 2026-07-14

Lookback: `git log --oneline -58` on branch `claude/weekly-indexing-28rbpm`, filtered to exclude
this run's own `chore(indexing-pass): checkpoint/finalize ...` commits (18 of them), leaving the
last **40 real content commits** (back to `5e1cc43`, 2026-07-06). Fix signatures searched:
internal-link adds (new `href`/`href:` targets in `src/`), sitemap adds, canonical/redirect
changes, and substantive content adds — matched against the 197 non-indexed URLs from today's
sweep (`2026-07-14.inspection.json`).

| url | fix_type | evidence_sha | commit_subject | source |
|---|---|---|---|---|
| https://www.themodernwallet.com/portfolio/asset-allocation-calculator/ | internal link | `edbdb6d` | ga4-top-pages-pass 2026-07-13: 5 new pages, 4 enrichments, 2 metadata across 9 audited winners | commit (`src/data/comparisons.ts` — `calculatorLinks` retarget from `/compare/stocks-vs-bonds/` and its sibling to this page) |

No other non-indexed URL from today's sweep had a matching fix in the last 40 commits. All other
actionable rows are diagnosed fresh in `2026-07-14.md` (root-cause found and fixed live in this
run's working tree — see that report's `fix` column).
