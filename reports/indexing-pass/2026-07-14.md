---
indexing_pass_date: 2026-07-14
source: GSC URL Inspection API sweep (reports/indexing-pass/2026-07-14.inspection.json)
base_url: https://www.themodernwallet.com
site_url: sc-domain:themodernwallet.com
generated_by: indexing-issues-gsc-pass build phase — .claude/tools/indexing-issues-gsc-pass/gsc_url_inspection.py
status: ready-for-execution
---

## Coverage note

The tool's hardcoded `/sitemap.xml` probe 404s on this Astro site — the real sitemap is
`sitemap-index.xml` -> `sitemap-0.xml` (via `@astrojs/sitemap`). All 460 URLs from
`sitemap-0.xml` were fetched directly and gap-filled into the sweep via `--urls-file --resume`.
Coverage is complete: 460/460 inspected, 0 deferred, not partial.

## Actionable URLs (197)

| url | coverage_state | diagnosis | fix | bucket | target |
|---|---|---|---|---|---|
| https://www.themodernwallet.com/compare/15-year-vs-20-year-vs-30-year-mortgage/ | URL is unknown to Google | Orphaned comparison page: no /compare/ index page existed at all (breadcrumb JSON-LD even referenced a URL that 404s), and relatedComparisons is purely manual/curated with no fallback — pages not cited by another indexed comparison had zero inbound links. checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: created /compare/ index (lists all 56 comparisons) + added sitewide footer link to it. | add internal links | src/pages/compare/index.astro; src/components/SiteFooter.astro |
| https://www.themodernwallet.com/compare/annuity-vs-cd/ | URL is unknown to Google | Orphaned comparison page: no /compare/ index page existed at all (breadcrumb JSON-LD even referenced a URL that 404s), and relatedComparisons is purely manual/curated with no fallback — pages not cited by another indexed comparison had zero inbound links. checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: created /compare/ index (lists all 56 comparisons) + added sitewide footer link to it. | add internal links | src/pages/compare/index.astro; src/components/SiteFooter.astro |
| https://www.themodernwallet.com/compare/business-line-of-credit-vs-sba-loan/ | URL is unknown to Google | Orphaned comparison page: no /compare/ index page existed at all (breadcrumb JSON-LD even referenced a URL that 404s), and relatedComparisons is purely manual/curated with no fallback — pages not cited by another indexed comparison had zero inbound links. checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: created /compare/ index (lists all 56 comparisons) + added sitewide footer link to it. | add internal links | src/pages/compare/index.astro; src/components/SiteFooter.astro |
| https://www.themodernwallet.com/compare/cash-out-refinance-vs-heloc/ | URL is unknown to Google | Orphaned comparison page: no /compare/ index page existed at all (breadcrumb JSON-LD even referenced a URL that 404s), and relatedComparisons is purely manual/curated with no fallback — pages not cited by another indexed comparison had zero inbound links. checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: created /compare/ index (lists all 56 comparisons) + added sitewide footer link to it. | add internal links | src/pages/compare/index.astro; src/components/SiteFooter.astro |
| https://www.themodernwallet.com/compare/cd-vs-money-market/ | URL is unknown to Google | Orphaned comparison page: no /compare/ index page existed at all (breadcrumb JSON-LD even referenced a URL that 404s), and relatedComparisons is purely manual/curated with no fallback — pages not cited by another indexed comparison had zero inbound links. checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: created /compare/ index (lists all 56 comparisons) + added sitewide footer link to it. | add internal links | src/pages/compare/index.astro; src/components/SiteFooter.astro |
| https://www.themodernwallet.com/compare/custodial-account-vs-savings-account/ | URL is unknown to Google | Orphaned comparison page: no /compare/ index page existed at all (breadcrumb JSON-LD even referenced a URL that 404s), and relatedComparisons is purely manual/curated with no fallback — pages not cited by another indexed comparison had zero inbound links. checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: created /compare/ index (lists all 56 comparisons) + added sitewide footer link to it. | add internal links | src/pages/compare/index.astro; src/components/SiteFooter.astro |
| https://www.themodernwallet.com/compare/elder-law-attorney-vs-estate-planning-attorney/ | URL is unknown to Google | Orphaned comparison page: no /compare/ index page existed at all (breadcrumb JSON-LD even referenced a URL that 404s), and relatedComparisons is purely manual/curated with no fallback — pages not cited by another indexed comparison had zero inbound links. checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: created /compare/ index (lists all 56 comparisons) + added sitewide footer link to it. | add internal links | src/pages/compare/index.astro; src/components/SiteFooter.astro |
| https://www.themodernwallet.com/compare/factor-rate-vs-interest-rate/ | URL is unknown to Google | Orphaned comparison page: no /compare/ index page existed at all (breadcrumb JSON-LD even referenced a URL that 404s), and relatedComparisons is purely manual/curated with no fallback — pages not cited by another indexed comparison had zero inbound links. checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: created /compare/ index (lists all 56 comparisons) + added sitewide footer link to it. | add internal links | src/pages/compare/index.astro; src/components/SiteFooter.astro |
| https://www.themodernwallet.com/compare/fha-loan-vs-va-loan/ | URL is unknown to Google | Orphaned comparison page: no /compare/ index page existed at all (breadcrumb JSON-LD even referenced a URL that 404s), and relatedComparisons is purely manual/curated with no fallback — pages not cited by another indexed comparison had zero inbound links. checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: created /compare/ index (lists all 56 comparisons) + added sitewide footer link to it. | add internal links | src/pages/compare/index.astro; src/components/SiteFooter.astro |
| https://www.themodernwallet.com/compare/fha-vs-conventional-loan/ | URL is unknown to Google | Orphaned comparison page: no /compare/ index page existed at all (breadcrumb JSON-LD even referenced a URL that 404s), and relatedComparisons is purely manual/curated with no fallback — pages not cited by another indexed comparison had zero inbound links. checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: created /compare/ index (lists all 56 comparisons) + added sitewide footer link to it. | add internal links | src/pages/compare/index.astro; src/components/SiteFooter.astro |
| https://www.themodernwallet.com/compare/gusto-401k-vs-paychex-401k/ | URL is unknown to Google | Orphaned comparison page: no /compare/ index page existed at all (breadcrumb JSON-LD even referenced a URL that 404s), and relatedComparisons is purely manual/curated with no fallback — pages not cited by another indexed comparison had zero inbound links. checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: created /compare/ index (lists all 56 comparisons) + added sitewide footer link to it. | add internal links | src/pages/compare/index.astro; src/components/SiteFooter.astro |
| https://www.themodernwallet.com/compare/heloc-vs-home-equity-loan/ | URL is unknown to Google | Orphaned comparison page: no /compare/ index page existed at all (breadcrumb JSON-LD even referenced a URL that 404s), and relatedComparisons is purely manual/curated with no fallback — pages not cited by another indexed comparison had zero inbound links. checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: created /compare/ index (lists all 56 comparisons) + added sitewide footer link to it. | add internal links | src/pages/compare/index.astro; src/components/SiteFooter.astro |
| https://www.themodernwallet.com/compare/heloc-vs-personal-loan/ | URL is unknown to Google | Orphaned comparison page: no /compare/ index page existed at all (breadcrumb JSON-LD even referenced a URL that 404s), and relatedComparisons is purely manual/curated with no fallback — pages not cited by another indexed comparison had zero inbound links. checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: created /compare/ index (lists all 56 comparisons) + added sitewide footer link to it. | add internal links | src/pages/compare/index.astro; src/components/SiteFooter.astro |
| https://www.themodernwallet.com/compare/index-fund-vs-etf/ | URL is unknown to Google | Orphaned comparison page: no /compare/ index page existed at all (breadcrumb JSON-LD even referenced a URL that 404s), and relatedComparisons is purely manual/curated with no fallback — pages not cited by another indexed comparison had zero inbound links. checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: created /compare/ index (lists all 56 comparisons) + added sitewide footer link to it. | add internal links | src/pages/compare/index.astro; src/components/SiteFooter.astro |
| https://www.themodernwallet.com/compare/index-fund-vs-mutual-fund/ | URL is unknown to Google | Orphaned comparison page: no /compare/ index page existed at all (breadcrumb JSON-LD even referenced a URL that 404s), and relatedComparisons is purely manual/curated with no fallback — pages not cited by another indexed comparison had zero inbound links. checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: created /compare/ index (lists all 56 comparisons) + added sitewide footer link to it. | add internal links | src/pages/compare/index.astro; src/components/SiteFooter.astro |
| https://www.themodernwallet.com/compare/invoice-factoring-vs-invoice-discounting/ | URL is unknown to Google | Orphaned comparison page: no /compare/ index page existed at all (breadcrumb JSON-LD even referenced a URL that 404s), and relatedComparisons is purely manual/curated with no fallback — pages not cited by another indexed comparison had zero inbound links. checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: created /compare/ index (lists all 56 comparisons) + added sitewide footer link to it. | add internal links | src/pages/compare/index.astro; src/components/SiteFooter.astro |
| https://www.themodernwallet.com/compare/living-trust-vs-lady-bird-deed/ | URL is unknown to Google | Orphaned comparison page: no /compare/ index page existed at all (breadcrumb JSON-LD even referenced a URL that 404s), and relatedComparisons is purely manual/curated with no fallback — pages not cited by another indexed comparison had zero inbound links. checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: created /compare/ index (lists all 56 comparisons) + added sitewide footer link to it. | add internal links | src/pages/compare/index.astro; src/components/SiteFooter.astro |
| https://www.themodernwallet.com/compare/pension-vs-401k/ | URL is unknown to Google | Orphaned comparison page: no /compare/ index page existed at all (breadcrumb JSON-LD even referenced a URL that 404s), and relatedComparisons is purely manual/curated with no fallback — pages not cited by another indexed comparison had zero inbound links. checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: created /compare/ index (lists all 56 comparisons) + added sitewide footer link to it. | add internal links | src/pages/compare/index.astro; src/components/SiteFooter.astro |
| https://www.themodernwallet.com/compare/power-of-attorney-vs-guardianship/ | URL is unknown to Google | Orphaned comparison page: no /compare/ index page existed at all (breadcrumb JSON-LD even referenced a URL that 404s), and relatedComparisons is purely manual/curated with no fallback — pages not cited by another indexed comparison had zero inbound links. checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: created /compare/ index (lists all 56 comparisons) + added sitewide footer link to it. | add internal links | src/pages/compare/index.astro; src/components/SiteFooter.astro |
| https://www.themodernwallet.com/compare/prenup-vs-postnup/ | URL is unknown to Google | Orphaned comparison page: no /compare/ index page existed at all (breadcrumb JSON-LD even referenced a URL that 404s), and relatedComparisons is purely manual/curated with no fallback — pages not cited by another indexed comparison had zero inbound links. checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: created /compare/ index (lists all 56 comparisons) + added sitewide footer link to it. | add internal links | src/pages/compare/index.astro; src/components/SiteFooter.astro |
| https://www.themodernwallet.com/compare/probate-vs-trust/ | URL is unknown to Google | Orphaned comparison page: no /compare/ index page existed at all (breadcrumb JSON-LD even referenced a URL that 404s), and relatedComparisons is purely manual/curated with no fallback — pages not cited by another indexed comparison had zero inbound links. checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: created /compare/ index (lists all 56 comparisons) + added sitewide footer link to it. | add internal links | src/pages/compare/index.astro; src/components/SiteFooter.astro |
| https://www.themodernwallet.com/compare/roth-ira-vs-traditional-ira/ | URL is unknown to Google | Orphaned comparison page: no /compare/ index page existed at all (breadcrumb JSON-LD even referenced a URL that 404s), and relatedComparisons is purely manual/curated with no fallback — pages not cited by another indexed comparison had zero inbound links. checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: created /compare/ index (lists all 56 comparisons) + added sitewide footer link to it. | add internal links | src/pages/compare/index.astro; src/components/SiteFooter.astro |
| https://www.themodernwallet.com/compare/secured-vs-unsecured-loan/ | URL is unknown to Google | Orphaned comparison page: no /compare/ index page existed at all (breadcrumb JSON-LD even referenced a URL that 404s), and relatedComparisons is purely manual/curated with no fallback — pages not cited by another indexed comparison had zero inbound links. checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: created /compare/ index (lists all 56 comparisons) + added sitewide footer link to it. | add internal links | src/pages/compare/index.astro; src/components/SiteFooter.astro |
| https://www.themodernwallet.com/compare/sep-ira-vs-solo-401k/ | URL is unknown to Google | Orphaned comparison page: no /compare/ index page existed at all (breadcrumb JSON-LD even referenced a URL that 404s), and relatedComparisons is purely manual/curated with no fallback — pages not cited by another indexed comparison had zero inbound links. checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: created /compare/ index (lists all 56 comparisons) + added sitewide footer link to it. | add internal links | src/pages/compare/index.astro; src/components/SiteFooter.astro |
| https://www.themodernwallet.com/compare/simple-ira-vs-401k/ | URL is unknown to Google | Orphaned comparison page: no /compare/ index page existed at all (breadcrumb JSON-LD even referenced a URL that 404s), and relatedComparisons is purely manual/curated with no fallback — pages not cited by another indexed comparison had zero inbound links. checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: created /compare/ index (lists all 56 comparisons) + added sitewide footer link to it. | add internal links | src/pages/compare/index.astro; src/components/SiteFooter.astro |
| https://www.themodernwallet.com/compare/stocks-vs-bonds-vs-cash/ | URL is unknown to Google | Orphaned comparison page: no /compare/ index page existed at all (breadcrumb JSON-LD even referenced a URL that 404s), and relatedComparisons is purely manual/curated with no fallback — pages not cited by another indexed comparison had zero inbound links. checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: created /compare/ index (lists all 56 comparisons) + added sitewide footer link to it. | add internal links | src/pages/compare/index.astro; src/components/SiteFooter.astro |
| https://www.themodernwallet.com/compare/traditional-ira-vs-401k/ | URL is unknown to Google | Orphaned comparison page: no /compare/ index page existed at all (breadcrumb JSON-LD even referenced a URL that 404s), and relatedComparisons is purely manual/curated with no fallback — pages not cited by another indexed comparison had zero inbound links. checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: created /compare/ index (lists all 56 comparisons) + added sitewide footer link to it. | add internal links | src/pages/compare/index.astro; src/components/SiteFooter.astro |
| https://www.themodernwallet.com/compare/whole-life-vs-term-life-insurance/ | URL is unknown to Google | Orphaned comparison page: no /compare/ index page existed at all (breadcrumb JSON-LD even referenced a URL that 404s), and relatedComparisons is purely manual/curated with no fallback — pages not cited by another indexed comparison had zero inbound links. checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: created /compare/ index (lists all 56 comparisons) + added sitewide footer link to it. | add internal links | src/pages/compare/index.astro; src/components/SiteFooter.astro |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/alaska/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/elder-care/medicaid-spend-down-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/arkansas/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/elder-care/medicaid-spend-down-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/colorado/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/elder-care/medicaid-spend-down-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/delaware/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/elder-care/medicaid-spend-down-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/georgia/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/elder-care/medicaid-spend-down-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/illinois/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/elder-care/medicaid-spend-down-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/iowa/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/elder-care/medicaid-spend-down-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/kansas/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/elder-care/medicaid-spend-down-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/kentucky/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/elder-care/medicaid-spend-down-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/louisiana/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/elder-care/medicaid-spend-down-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/maine/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/elder-care/medicaid-spend-down-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/maryland/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/elder-care/medicaid-spend-down-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/massachusetts/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/elder-care/medicaid-spend-down-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/minnesota/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/elder-care/medicaid-spend-down-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/mississippi/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/elder-care/medicaid-spend-down-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/missouri/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/elder-care/medicaid-spend-down-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/montana/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/elder-care/medicaid-spend-down-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/nevada/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/elder-care/medicaid-spend-down-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/new-hampshire/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/elder-care/medicaid-spend-down-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/new-jersey/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/elder-care/medicaid-spend-down-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/new-mexico/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/elder-care/medicaid-spend-down-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/new-york/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/elder-care/medicaid-spend-down-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/north-carolina/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/elder-care/medicaid-spend-down-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/ohio/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/elder-care/medicaid-spend-down-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/oklahoma/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/elder-care/medicaid-spend-down-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/oregon/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/elder-care/medicaid-spend-down-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/rhode-island/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/elder-care/medicaid-spend-down-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/south-carolina/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/elder-care/medicaid-spend-down-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/tennessee/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/elder-care/medicaid-spend-down-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/texas/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/elder-care/medicaid-spend-down-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/utah/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/elder-care/medicaid-spend-down-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/vermont/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/elder-care/medicaid-spend-down-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/virginia/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/elder-care/medicaid-spend-down-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/washington/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/elder-care/medicaid-spend-down-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/west-virginia/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/elder-care/medicaid-spend-down-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/elder-care/medicaid-spend-down-calculator/wisconsin/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/elder-care/medicaid-spend-down-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/estate-planning/will-cost-calculator/georgia/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/estate-planning/will-cost-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/estate-planning/will-cost-calculator/idaho/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/estate-planning/will-cost-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/estate-planning/will-cost-calculator/indiana/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/estate-planning/will-cost-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/estate-planning/will-cost-calculator/kansas/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/estate-planning/will-cost-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/estate-planning/will-cost-calculator/kentucky/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/estate-planning/will-cost-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/estate-planning/will-cost-calculator/massachusetts/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/estate-planning/will-cost-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/estate-planning/will-cost-calculator/michigan/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/estate-planning/will-cost-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/estate-planning/will-cost-calculator/mississippi/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/estate-planning/will-cost-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/estate-planning/will-cost-calculator/montana/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/estate-planning/will-cost-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/estate-planning/will-cost-calculator/new-hampshire/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/estate-planning/will-cost-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/estate-planning/will-cost-calculator/new-jersey/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/estate-planning/will-cost-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/estate-planning/will-cost-calculator/new-york/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/estate-planning/will-cost-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/estate-planning/will-cost-calculator/north-carolina/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/estate-planning/will-cost-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/estate-planning/will-cost-calculator/ohio/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/estate-planning/will-cost-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/estate-planning/will-cost-calculator/rhode-island/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/estate-planning/will-cost-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/estate-planning/will-cost-calculator/south-dakota/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/estate-planning/will-cost-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/estate-planning/will-cost-calculator/texas/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/estate-planning/will-cost-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/estate-planning/will-cost-calculator/virginia/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/estate-planning/will-cost-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/estate-planning/will-cost-calculator/west-virginia/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/estate-planning/will-cost-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/estate-planning/will-cost-calculator/wisconsin/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/estate-planning/will-cost-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/estate-planning/will-cost-calculator/wyoming/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/estate-planning/will-cost-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/investing/investment-growth-calculator/ | URL is unknown to Google | Not orphaned — already has ≥3 inbound links (from its category hub page + sibling relatedSpokes cross-links). Per Phase 6's already-linked guard, no new links warranted; likely a crawl-budget/priority timing issue on a 460-URL site, not a linking defect. | No new links added (already-linked guard: ≥3 inbound present). Re-submit via Phase 7 only. | add internal links | n/a — already linked |
| https://www.themodernwallet.com/merchant-cash-advance/merchant-cash-advance-payoff-calculator/ | URL is unknown to Google | Not orphaned — already has ≥3 inbound links (from its category hub page + sibling relatedSpokes cross-links). Per Phase 6's already-linked guard, no new links warranted; likely a crawl-budget/priority timing issue on a 460-URL site, not a linking defect. | No new links added (already-linked guard: ≥3 inbound present). Re-submit via Phase 7 only. | add internal links | n/a — already linked |
| https://www.themodernwallet.com/mortgage/down-payment-calculator/ | URL is unknown to Google | Not orphaned — already has ≥3 inbound links (from its category hub page + sibling relatedSpokes cross-links). Per Phase 6's already-linked guard, no new links warranted; likely a crawl-budget/priority timing issue on a 460-URL site, not a linking defect. | No new links added (already-linked guard: ≥3 inbound present). Re-submit via Phase 7 only. | add internal links | n/a — already linked |
| https://www.themodernwallet.com/mortgage/early-payoff-calculator/ | URL is unknown to Google | Not orphaned — already has ≥3 inbound links (from its category hub page + sibling relatedSpokes cross-links). Per Phase 6's already-linked guard, no new links warranted; likely a crawl-budget/priority timing issue on a 460-URL site, not a linking defect. | No new links added (already-linked guard: ≥3 inbound present). Re-submit via Phase 7 only. | add internal links | n/a — already linked |
| https://www.themodernwallet.com/mortgage/piti-calculator/ | URL is unknown to Google | Not orphaned — already has ≥3 inbound links (from its category hub page + sibling relatedSpokes cross-links). Per Phase 6's already-linked guard, no new links warranted; likely a crawl-budget/priority timing issue on a 460-URL site, not a linking defect. | No new links added (already-linked guard: ≥3 inbound present). Re-submit via Phase 7 only. | add internal links | n/a — already linked |
| https://www.themodernwallet.com/portfolio/asset-allocation-calculator/ | URL is unknown to Google | ✅ shipped in `edbdb6d` (ga4-top-pages-pass 2026-07-13) — calculatorLinks retarget added inbound link from /compare/stocks-vs-bonds/ (+sibling) to this page; checked ledger (HEAD..HEAD~58 excl. this run's own commits). | No action — fix already shipped; awaiting Google recrawl. | already-fixed — awaiting recrawl | src/data/comparisons.ts |
| https://www.themodernwallet.com/probate/fee-calculator/alabama/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/fee-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/fee-calculator/alaska/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/fee-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/fee-calculator/arizona/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/fee-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/fee-calculator/california/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/fee-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/fee-calculator/delaware/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/fee-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/fee-calculator/florida/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/fee-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/fee-calculator/georgia/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/fee-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/fee-calculator/hawaii/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/fee-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/fee-calculator/idaho/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/fee-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/fee-calculator/illinois/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/fee-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/fee-calculator/indiana/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/fee-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/fee-calculator/iowa/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/fee-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/fee-calculator/kansas/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/fee-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/fee-calculator/kentucky/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/fee-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/fee-calculator/louisiana/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/fee-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/fee-calculator/maryland/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/fee-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/fee-calculator/massachusetts/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/fee-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/fee-calculator/michigan/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/fee-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/fee-calculator/minnesota/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/fee-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/fee-calculator/mississippi/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/fee-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/fee-calculator/missouri/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/fee-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/fee-calculator/montana/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/fee-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/fee-calculator/nebraska/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/fee-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/fee-calculator/nevada/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/fee-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/fee-calculator/new-hampshire/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/fee-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/fee-calculator/new-jersey/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/fee-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/fee-calculator/new-mexico/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/fee-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/fee-calculator/new-york/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/fee-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/fee-calculator/north-carolina/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/fee-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/fee-calculator/north-dakota/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/fee-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/fee-calculator/ohio/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/fee-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/fee-calculator/oklahoma/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/fee-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/fee-calculator/oregon/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/fee-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/fee-calculator/pennsylvania/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/fee-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/fee-calculator/tennessee/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/fee-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/fee-calculator/texas/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/fee-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/fee-calculator/utah/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/fee-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/fee-calculator/vermont/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/fee-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/fee-calculator/virginia/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/fee-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/fee-calculator/washington/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/fee-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/fee-calculator/west-virginia/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/fee-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/fee-calculator/wisconsin/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/fee-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/fee-calculator/wyoming/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/fee-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/timeline-calculator/arkansas/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/timeline-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/timeline-calculator/california/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/timeline-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/timeline-calculator/colorado/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/timeline-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/timeline-calculator/connecticut/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/timeline-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/timeline-calculator/delaware/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/timeline-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/timeline-calculator/florida/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/timeline-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/timeline-calculator/georgia/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/timeline-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/timeline-calculator/hawaii/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/timeline-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/timeline-calculator/idaho/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/timeline-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/timeline-calculator/illinois/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/timeline-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/timeline-calculator/indiana/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/timeline-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/timeline-calculator/iowa/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/timeline-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/timeline-calculator/kansas/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/timeline-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/timeline-calculator/kentucky/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/timeline-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/timeline-calculator/louisiana/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/timeline-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/timeline-calculator/maine/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/timeline-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/timeline-calculator/maryland/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/timeline-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/timeline-calculator/massachusetts/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/timeline-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/timeline-calculator/minnesota/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/timeline-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/timeline-calculator/mississippi/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/timeline-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/timeline-calculator/missouri/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/timeline-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/timeline-calculator/montana/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/timeline-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/timeline-calculator/nebraska/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/timeline-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/timeline-calculator/nevada/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/timeline-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/timeline-calculator/new-hampshire/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/timeline-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/timeline-calculator/new-jersey/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/timeline-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/timeline-calculator/new-york/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/timeline-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/timeline-calculator/north-carolina/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/timeline-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/timeline-calculator/north-dakota/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/timeline-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/timeline-calculator/ohio/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/timeline-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/timeline-calculator/oklahoma/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/timeline-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/timeline-calculator/pennsylvania/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/timeline-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/timeline-calculator/rhode-island/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/timeline-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/timeline-calculator/south-carolina/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/timeline-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/timeline-calculator/south-dakota/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/timeline-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/timeline-calculator/tennessee/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/timeline-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/timeline-calculator/texas/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/timeline-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/timeline-calculator/vermont/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/timeline-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/timeline-calculator/washington/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/timeline-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/timeline-calculator/west-virginia/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/timeline-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/timeline-calculator/wisconsin/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/timeline-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/probate/timeline-calculator/wyoming/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/probate/timeline-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/real-estate/roi-calculator/ | URL is unknown to Google | Not orphaned — already has ≥3 inbound links (from its category hub page + sibling relatedSpokes cross-links). Per Phase 6's already-linked guard, no new links warranted; likely a crawl-budget/priority timing issue on a 460-URL site, not a linking defect. | No new links added (already-linked guard: ≥3 inbound present). Re-submit via Phase 7 only. | add internal links | n/a — already linked |
| https://www.themodernwallet.com/retirement/ira-early-withdrawal-calculator/ | URL is unknown to Google | Not orphaned — already has ≥3 inbound links (from its category hub page + sibling relatedSpokes cross-links). Per Phase 6's already-linked guard, no new links warranted; likely a crawl-budget/priority timing issue on a 460-URL site, not a linking defect. | No new links added (already-linked guard: ≥3 inbound present). Re-submit via Phase 7 only. | add internal links | n/a — already linked |
| https://www.themodernwallet.com/roundup/best-budgeting-apps/ | URL is unknown to Google | Orphaned roundup page: zero inbound links; its sibling best-budgeting-apps-for-couples links to it, but via a raw-HTML string a pre-existing bug that gets HTML-escaped by linkify() and rendered as inert text, not a real link. checked ledger (HEAD..HEAD~58), no prior fix (the sibling link was never functional). | Fixed this run: corrected the broken raw-HTML link to markdown syntax + added a new reciprocal link from the couples variant. | add internal links | src/data/roundups.ts |
| https://www.themodernwallet.com/roundup/best-invoice-factoring-companies/ | URL is unknown to Google | Orphaned roundup page: no inbound links anywhere on the site (footer only curates 5 fixed roundups; no /roundup/ index exists). Topically an exact match for the /invoice-factoring/ hub. checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a natural contextual link from the /invoice-factoring/ hub ("Comparing providers? See our best-of roundup"). | add internal links | src/pages/[category]/index.astro |
| https://www.themodernwallet.com/roundup/best-merchant-cash-advance-companies/ | URL is unknown to Google | Orphaned roundup page: no inbound links anywhere on the site (footer only curates 5 fixed roundups; no /roundup/ index exists). Topically an exact match for the /merchant-cash-advance/ hub. checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a natural contextual link from the /merchant-cash-advance/ hub ("Comparing providers? See our best-of roundup"). | add internal links | src/pages/[category]/index.astro |
| https://www.themodernwallet.com/tax-resolution/offer-in-compromise-calculator/kentucky/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/tax-resolution/offer-in-compromise-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/tax-resolution/offer-in-compromise-calculator/louisiana/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/tax-resolution/offer-in-compromise-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/tax-resolution/offer-in-compromise-calculator/maryland/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/tax-resolution/offer-in-compromise-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/tax-resolution/offer-in-compromise-calculator/michigan/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/tax-resolution/offer-in-compromise-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/tax-resolution/offer-in-compromise-calculator/minnesota/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/tax-resolution/offer-in-compromise-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/tax-resolution/offer-in-compromise-calculator/mississippi/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/tax-resolution/offer-in-compromise-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/tax-resolution/offer-in-compromise-calculator/missouri/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/tax-resolution/offer-in-compromise-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/tax-resolution/offer-in-compromise-calculator/new-jersey/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/tax-resolution/offer-in-compromise-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/tax-resolution/offer-in-compromise-calculator/new-york/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/tax-resolution/offer-in-compromise-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/tax-resolution/offer-in-compromise-calculator/north-carolina/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/tax-resolution/offer-in-compromise-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/tax-resolution/offer-in-compromise-calculator/ohio/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/tax-resolution/offer-in-compromise-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/tax-resolution/offer-in-compromise-calculator/oklahoma/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/tax-resolution/offer-in-compromise-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/tax-resolution/offer-in-compromise-calculator/pennsylvania/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/tax-resolution/offer-in-compromise-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/tax-resolution/offer-in-compromise-calculator/south-carolina/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/tax-resolution/offer-in-compromise-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/tax-resolution/offer-in-compromise-calculator/texas/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/tax-resolution/offer-in-compromise-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |
| https://www.themodernwallet.com/tax-resolution/offer-in-compromise-calculator/wisconsin/ | URL is unknown to Google | Orphaned state-variant page: the parent overview page (/tax-resolution/offer-in-compromise-calculator/) never linked out to its 50 state sub-pages (state pages only link to each other, not from the hub). checked ledger (HEAD..HEAD~58), no prior fix. | Fixed this run: added a 'Browse by state' link module to the category overview template (all 50 states, matches existing sister-states pattern). | add internal links | src/pages/[category]/[slug].astro |

## Exclusions (263 healthy — not actionable)
263 URLs returned `Submitted and indexed` (verdict PASS) and are excluded from the table above —
no action needed.

## Buckets seen
- `add internal links`: 196 (158 state-variant pages across 5 clusters; 28 comparison pages; 7
  singles already sufficiently linked — no new links added; 3 roundup singles fixed with a
  targeted contextual link)
- `already-fixed — awaiting recrawl`: 1 (`/portfolio/asset-allocation-calculator/`, shipped in
  `edbdb6d`, 2026-07-13)
- No canonical/duplicate, redirect/error, indexability, or thin-content issues found — every
  actionable row this sweep is a discovery/internal-linking gap.

## Root causes found + fixed this run
1. **State-variant hub pages never linked to their state sub-pages** (158 pages, 5 clusters:
   probate/fee-calculator, probate/timeline-calculator, elder-care/medicaid-spend-down-calculator,
   estate-planning/will-cost-calculator, tax-resolution/offer-in-compromise-calculator). State
   pages link to each other but the parent overview page had zero links out to them. Fixed by
   adding a "Browse by state" section to `src/pages/[category]/[slug].astro`.
2. **No `/compare/` index page existed at all** (28 orphaned comparison pages; the breadcrumb
   JSON-LD on every comparison page even referenced `/compare/` as a URL that 404s).
   `relatedComparisons` is purely manual/curated with no algorithmic fallback, so any comparison
   not cited by another indexed comparison was unreachable except via the sitemap. Fixed by adding
   `src/pages/compare/index.astro` (lists all 56 comparisons) + a sitewide footer link to it.
3. **2 roundup pages topically tied to a hub had no link from that hub**
   (`best-merchant-cash-advance-companies`, `best-invoice-factoring-companies`). Fixed with a
   natural, topically-exact contextual link from the matching category hub page.
4. **1 roundup page's only "inbound link" was inert** — `best-budgeting-apps`'s sibling page
   referenced it via raw HTML embedded in a content string, which the site's `linkify()` helper
   HTML-escapes (renders as literal escaped text, not a link) since it expects markdown
   `[text](url)` syntax. Fixed the syntax + added the reciprocal link.
5. **7 singles were already adequately linked** (from their category hub + sibling
   `relatedSpokes` cross-links) — the already-linked guard applied, no new links added; these are
   left for natural crawl-budget/priority timing, not a linking defect.
