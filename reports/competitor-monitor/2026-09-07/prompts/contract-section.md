Return ONLY markdown: one `## ` heading (Title Case, states the finding, no headline tells) that
does NOT duplicate any existing heading on the page (the page's current text is provided), then
one to three paragraphs of prose, separated by blank lines. Do not include an H1. Do not include
any JSON, code fences, or explanation of what you are doing. Every external link must be a
markdown link `[text](url)` whose `url` is on the CLOSED URL LIST in the row prompt. Every number,
date, or limit you state must come from the row prompt's CLOSED FACT LIST — never invent one.
