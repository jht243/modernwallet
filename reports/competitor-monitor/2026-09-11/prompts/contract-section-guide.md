# Output contract — a section addition to an EXISTING guide page (ModernWallet, guides.ts)

Return PLAIN MARKDOWN, not JSON, not a code fence:
- Output exactly one `## Heading In Title Case` line, followed by one or more paragraphs (blank line between paragraphs).
- Markdown links `[text](url)` for every external company/source/regulator named — first mention only, official/primary source only, from the closed URL list.
- Match the voice of the sample: operator register, "you" addressing the reader, plain declarative sentences, varied sentence length, no filler.
- This page's existing prose never uses "we" — it addresses the reader directly as "you" throughout with no first-person brand voice. Match that: do NOT introduce a new "At ModernWallet, we..." line or any other first-person "we" claim in this addition.
- Do not add a disclaimer, a CTA, or a "related guides" list — those are handled elsewhere on the page.
- Internal links use only the exact paths given in the task's internal-links list, if any.
