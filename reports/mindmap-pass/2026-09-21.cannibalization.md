# Phase 1 — Cannibalization detection (ADVISORY ONLY) — mindmap chart 2026-09-21

**Chart consolidation rows: 0.** No chart row was classified `consolidate / canonicalize`.

## Independent scan (existing pages vs the topics this run adds)

Scanned `src/data/*.ts` for any existing page pair competing for the GRAT / gift-tax / IDGT / SLAT / Form 709 / 7520 clusters:

- `grep -rliE "grat\b|grantor.retained|annuity trust" src/` → 1 file, 1 page: `advanced-estate-planning-strategies` (guides.ts).
- `grep -rhoE "slug: \"[^\"]*(gift|7520|idgt|slat|grat)[^\"]*\"" src/data` → 0 matches.
- No slug pair anywhere in `src/data` targets the same cluster as another.

**Result: no cannibalizing pair exists today.** Exactly one page has a dedicated section on any of these clusters (`advanced-estate-planning-strategies`). `grep -rliE "gift tax" src/` returns 4 files, but those are passing mentions inside pages targeting other head terms (verified against each page's `targetKeyword`), not competing coverage. Nothing competes with anything.

## Forward-looking note (not an action)

`/guides/advanced-estate-planning-strategies/` is a multi-tool pillar containing a GRAT section, an IDGT section, and a GRAT-vs-IDGT FAQ. This run adds dedicated pages for those same entities (`/guides/what-is-a-grat-trust/`, `/guides/intentionally-defective-grantor-trust-idgt/`, `/compare/grat-vs-idgt/`).

- Dominance read: **not applicable** — the pillar ranks as a broad "advanced estate planning strategies" page, not for the head terms "grat trust" (1,000/mo) or "idgt" (2,900/mo), which have no dedicated answer on this site.
- Recommendation: **differentiate, do not consolidate.** The pillar keeps its role as the multi-tool overview; each new page owns one entity's head term. Phase 5 adds the news/IRS-scrutiny angle to the pillar's GRAT section with a pointer out to the dedicated guide, and Phase 6 links pillar → spokes. That is differentiation + internal linking, the softer fix — no canonical, no 301.
- Re-check after ~8 weeks of GSC data: if the pillar and `/guides/what-is-a-grat-trust/` start trading places on the same queries, that is the moment for a human to look again.

## Files touched by this phase

**None.** No canonical, no 301, no body edit, no link re-point, no sitemap change.
