# Cannibalization advisory — mindmap-pass Coast FI brief (2026-09-09)

**ADVISORY ONLY. No source file was changed by this phase — no canonical, no 301, no body edit, no link re-point, no sitemap removal.**

Chart rows in the `consolidate / canonicalize` bucket: **0**. One competing pair was carried
forward from the chart's exclusions block for detection.

## Detected pairs

### Pair 1 — `/coast-fire/` vs `/guides/coast-fire-guide`

| | Route | Impressions | Clicks |
|---|---|---|---|
| A | `/coast-fire/` (src/data/calculators.ts, id `coast-fire`) | not available this run | not available this run |
| B | `/guides/coast-fire-guide` (src/data/guides.ts:2008) | not available this run | not available this run |

**Impressions/clicks are unavailable because this pass is brief-driven (a WSJ article), not
GSC-screenshot-driven — the chart carries keyword-demand and live-SERP data, not per-URL GSC
performance. Dominance therefore cannot be measured, which by itself forbids recommending a
consolidation.**

**Shared query cluster:** "how the Coast FIRE number is calculated" — both pages explain the
discount-formula derivation at length. `/coast-fire/` does it in `howItWorks`
(calculators.ts:819) and `/guides/coast-fire-guide` does it in the section "How the Coast FIRE
number is calculated" (guides.ts:2019). Overlapping tail: "how to calculate coast fire" (170/kd5),
"coast fire formula" (70/kd4), "what is my coast fire number" (390/kd7).

**Recommendation: DO NOT consolidate — differentiate instead.** Two reasons, either sufficient
on its own:
1. Dominance is unmeasured (no GSC data this run), so the ≥3× test cannot be satisfied. Absent
   that evidence this is treated as a near-tie.
2. The two pages serve genuinely different intents — `/coast-fire/` is the interactive tool
   (targetKeyword "coast fire calculator"), the guide is the explainer. The overlap is one
   section, not the page.

The safe fixes are already scheduled as separate chart rows, and this phase does not perform them:
- Phase 5 row 2 adds the "Coast FI / Coast FIRE are one milestone" gloss to the **guide**,
  pushing it further toward definitional intent.
- Phase 5 row 6 adds the inflation-drift section to the **guide** (editorial), while row 7 adds
  the HSA/emergency-fund FAQ to the **calculator** (input scope) — each landing on the page that
  owns that intent.
- Phase 6 row 8 adds reciprocal links with distinct anchor text so Google can tell the seven
  coast URLs apart after this pass.

**No pair on this chart is recommended for consolidation.**

## Prior-run context
`reports/keyword-pass/2026-07-29.cannibalization.md` recorded "No competing-pair cannibalization
detected this run" for the then-two-URL coast cluster. This pass grows that cluster from four
URLs to seven, which is why the Phase 6 link row exists.
