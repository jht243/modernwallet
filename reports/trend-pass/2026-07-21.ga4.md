# GA4 top-pages pass — 2026-07-21

## Phase 0 pull
Top pages (24h, all channels), property 543459487, data date 2026-07-21: `/` (1 view, 100% Direct channel). Total top-10 views: 1. Only 1 page registered any views tonight.

## Per-page verdict table
| page | views | non-organic % | audited or skipped (why) | actions |
|---|---|---|---|---|
| / | 1 | 100% (Direct) | AUDITED (last audited 2026-07-13, 8 days ago ≥ 7-day cooldown) | 0 (all lanes closed — no gap; brand-confusion signal flagged for human) |

## GSC evidence (Phase 1)
`/`: verdict **mixed** (15 impressions/30d, 0 clicks, CTR 0%, position 19.33, 30d impression trend "up"). `rewrite_recommended: true` but Lane D's trigger requires `underperforming` specifically — **Lane D (metadata) CLOSED.**

Top 3 GSC queries are all competitor-brand searches, not the site's own brand:
- "nerdwallet calculator" — 6 impressions, pos 29
- "nerd wallet mortgage calculator" — 2 impressions, pos 39
- "nerdwallet financial calculator" — 1 impression, pos 25

## Lane outcomes

### / (homepage)
- **Lane A (spoke):** N/A — the homepage is a hub/index, not a single product/account entity; no natural sibling set to build.
- **Lane B (comparison):** N/A — same reason; no single entity to compare.
- **Lane C (enrichment):** none. Autocomplete mining (383 suggestions across 4 seeds: "personal finance calculator" + the 3 GSC queries above) returned almost entirely (a) more permutations of the branded "nerdwallet" queries, or (b) foreign bank-specific personal-loan EMI queries (HDFC, SBI, Emirates NBD, etc.) irrelevant to this US-focused site. The generic "financial calculator" / "personal loan calculator" / "mortgage calculator" intent behind the branded queries is already served by the site's existing 11 mortgage calculators (`/mortgage/*`) and `/personal-loan/` calculator. The homepage's own title/meta ("ModernWallet — Free Financial Calculators") already leads with that positioning — no unanswered sub-intent to append.
- **Lane D (metadata):** CLOSED — verdict is `mixed`, not `underperforming`.
- **Lane E (tools):** none. No tool-intent gap found beyond what's already covered by existing calculators.

## Dedup (Phase 3)
Zero candidates were generated by Lanes A/B/C/E — nothing to run through Layers 1–3. An independent Layer-4 adversarial reviewer (fresh subagent session, did not build the audit) was still spawned to stress-test the "no gap" conclusion itself: it independently reproduced the autocomplete mining, checked the live site inventory and homepage content directly, and confirmed no missed candidate (CONFIRMED, no refutation).

## Flagged for the human (winner protection / brand safety)
The site's top 3 GSC queries this 30-day window are all "nerdwallet"-branded searches (9 total impressions, 0 clicks) — plausibly users confusing "themodernwallet.com" with the competitor "NerdWallet" given the similar name, or landing here after a NerdWallet-branded search. This pass deliberately did **not** build content targeting that competitor-brand query cluster (e.g. a "NerdWallet alternatives" or brand-vs-brand comparison page). The site does have one precedent for named-brand comparison content (`compare/gusto-401k-vs-paychex-401k`), but that compares two third-party products a reader is actually choosing between — a materially different fact pattern from building content around a competitor's brand name when the two site names are already easily confused. Judged a brand/trademark decision outside this pass's mechanical scope, not a normal content gap — routed to you rather than auto-built.

## Ledger deltas
- `reports/trend-pass/ledger.md`: 1 new row (source: ga4-top-pages), tagged 2026-07-21 — DROPPED (no gap) + FLAGGED, a whole-page disposition (Lane mining produced zero individual candidates).
- `reports/trend-pass/ga4-page-audits.md`: 1 new row (`/`, audited tonight), now on a fresh 7-day cooldown (next-eligible 2026-07-28).

## Deferred
None.

## Live verification
No pages were created or edited this run (no candidates survived mining) — nothing to 200-check beyond the routine data-recording commit itself, which touches no live routes.

## IndexNow
Not applicable — no new or changed URLs this run.

## Known issue (environment, not this run)
This run's Phase 0 commit (`bdadd84`) is unsigned — GitHub will show it as "Unverified." Investigated: the environment's SSH commit-signing key file (`~/.ssh/commit_signing_key.pub`) is empty (0 bytes), so `commit.gpgsign=true` silently produces no signature. Confirmed this affects every prior automated commit in this repo's history (checked several), so it predates this run and isn't specific to it. The commit is already an ancestor of other cron pushes to `main` (an `indexing-pass` run landed on top of it before this could be corrected), so fixing it now would require rewriting shared `main` history — not attempted; flagging for you to fix the signing-key provisioning at the environment level instead.
