# GA4 top-pages audit — 2026-08-24

## Phase 0 — GA4 pull
Top-10 pull returned a single page for the freshest available day (2026-08-24): thin GA4 volume across the site today.

| page | views | non-organic % |
|---|---|---|
| /roundup/best-401k-providers-for-small-business/ | 1 | 100% (Direct) |

## Per-page verdict table
| page | views | non-organic % | audited or skipped (why) | actions |
|---|---|---|---|---|
| /roundup/best-401k-providers-for-small-business/ | 1 | 100% Direct | AUDITED (last audited 2026-07-28, >7d, 0 deferred) | 1 FAQ enrichment (KEPT); 1 metadata rewrite (DROPPED — see below) |

(Only 1 page appeared in tonight's GA4 top-10 pull; no other pages to report as skipped-on-cooldown.)

## Pages shipped (N)
None this run — no new pages.

## Body & FAQ enrichments (1)
- **/roundup/best-401k-providers-for-small-business/** — new FAQ "What are the alternatives to a 401(k) for a small business?" (autocomplete sub-intent: "what are alternatives to 401k plans" / GSC query: "401k alternatives for small business"). Links the page's own existing but previously-unexplained `/compare/simple-ira-vs-401k/` and `/compare/sep-ira-vs-solo-401k/` comparisons into an actual answer.

## Metadata rewrites (0)
- **/roundup/best-401k-providers-for-small-business/** — DROPPED. GSC verdict was `underperforming`/`rewrite_recommended: true` (15 impressions/30d, CTR 0%, position 39.13, trending down), but every candidate query has 1-3 impressions/30d (noise) and CTR is mechanically ~0 at position 39 regardless of title — a rewrite can't move the metric that would justify it. Title/meta left unchanged.

## Tools built (0)
None this run — no tool-intent autocomplete signal for this page's topic.

## Deferred (0)
None.

## Flagged for the human (winner protection) (0)
None from winner protection. Separately flagged below (not a winner-protection case).

## Ledger deltas
2 rows appended to `reports/trend-pass/ledger.md` (1 KEPT, 1 DROPPED), tagged `source: ga4-top-pages`. 1 row appended to `reports/trend-pass/ga4-page-audits.md`.

## Audit
Layer 4 adversarial dedup review: PASS (both candidates independently reviewed; FAQ candidate KEPT, metadata candidate recommended KEEP-CURRENT — matches the mechanical Lane D verdict for different reasons: thin evidence + position-driven CTR floor).

Stage 4 content audit: round 1 FAILED — the FAQ answer contained an embedded `\n\n` paragraph break that this template's FAQ renderer (`src/pages/roundup/[slug].astro`, single `<p set:html=...>`) would have collapsed into one run-together paragraph in production; it was also the only FAQ in the file with an internal paragraph break, breaking format parity with sibling FAQs. Round 2 fix collapsed the answer to one continuous paragraph; re-verified directly in the built HTML output (`dist/roundup/best-401k-providers-for-small-business/index.html`) — renders correctly with both links intact. Round 2 PASS.

`npx tsc --noEmit -p .` clean. `npm run build` — 653 pages, clean, no reverts needed.

## IndexNow
Not submitted — this run only appended a section to an already-indexed, already-canonical URL; no new URL and no route change. (No IndexNow-eligible change.)

## Live verification
`/roundup/best-401k-providers-for-small-business/` → HTTP 200. New FAQ confirmed present in the live HTML (initially masked by a 5-minute Cloudflare edge cache — origin confirmed updated via a cache-busted request immediately, canonical URL confirmed serving the new content ~90s after the edge cache's `s-maxage=300` window rolled over). No revert needed.

## Flagged for the human — ranking collapse (not winner-protection)
`/roundup/best-401k-providers-for-small-business/` had 60-130 GSC impressions/day in early July 2026; by mid-to-late August that had collapsed to near-zero (15 impressions total over the trailing 30 days, avg position 39). This is a real ranking/indexing event outside this pass's content-only scope — worth a Search Console coverage/manual-action check and a look at what changed on or around this page in mid-July. Note: the site's sitemap is correctly served at `/sitemap-index.xml` per `robots.txt`; a probe against `/sitemap.xml` 404s by design (standard `@astrojs/sitemap` behavior), so that is NOT the cause and needs no fix.
