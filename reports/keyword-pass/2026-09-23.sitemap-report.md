# Phase 7 — Sitemap + IndexNow prep — 2026-09-23

## Sitemap
This repo has no static sitemap XML file (`@astrojs/sitemap` in `astro.config.mjs` auto-generates it at build time from actual page routes, filtered by `src/data/noindex.ts`, per the same setup documented in `reports/keyword-pass/2026-09-16.sitemap-report.md`). No manual sitemap edit is possible or needed.

Verified the 3 new routes will be picked up automatically:
- `src/pages/guides/[slug].astro` calls `getStaticPaths()` over the `GUIDES` array (`src/data/guides.ts`) — the 3 new guide entries (`529-plan-tax-deduction-by-state`, `free-cash-flow-calculator-and-dcf-valuation-explained`, `hysa-vs-roth-ira-vs-brokerage-account`) are included automatically.
- None of the 3 new slugs appear in `src/data/noindex.ts` — that file only de-indexes sub-pages under 7 specific pillar IDs (`tax-resolution`, `estate-planning`, `probate`, `elder-care`, `merchant-cash-advance`, `invoice-factoring`, `business-line-of-credit`), none of which apply to a `/guides/*` route.
- `lastmod` is assigned by the sitemap integration at build time (confirmed by the 2026-09-16 precedent: this integration does not read a per-page `updated` field), so every URL present in the sitemap at build time carries today's build timestamp — no manual lastmod bump needed or possible.
- Each page record's own `updated` field (byline/JSON-LD `dateModified`, separate from the sitemap) WAS bumped to `2026-09-23` on every page that received a substantive edit this run (the 7 body-text pages below); the 3 new guides were created with `"updated": "2026-09-23"` already set. Pages that received only a Phase-6 link-sentence addition were left untouched, per the "link-only touch is not a content change" rule.

## No interactive-tool route created
The `debt-snowball-calculator` new-content row produced a spec only (`reports/keyword-gap-pass/specs/debt-snowball-calculator-spec.md`), not a page — no `calculators.ts` entry and no route file, since the framework has no route to stub without a working island component. Nothing to add to the sitemap for this row.

## IndexNow URL list
Written to `reports/keyword-pass/2026-09-23.indexnow.txt` — 10 URLs:
- 3 new routes (Phase 3): the two new guide pages plus the one 529 guide
- 7 substantively-changed existing pages (Phase 5 FAQ additions; each also received a Phase 6 inbound/cross link, which counts as substantive per the "both a substantive edit AND a link = substantive" rule): `/cash-conversion-cycle/`, `/operating-cash-flow/`, `/529-savings-calculator/`, `/real-estate/cash-flow-calculator/`, `/investing/high-yield-savings-calculator/`, `/roundup/best-high-yield-savings-accounts/`, `/compare/debt-snowball-vs-avalanche/`

**Excluded** (link-only touch, Phase 6, no lastmod/IndexNow bump per the rule): `/guides/529-leftover-money-options/`, `/guides/529-qualified-expenses/`, `/guides/investment-appraisal-methods-npv-irr/`, `/guides/pay-off-debt-or-invest/`, `/compare/hysa-vs-money-market/`.

No consolidation losers this run (Phase 1 was advisory-only, 0 pairs). No legacy `.consolidation.txt` file exists for 2026-09-23.

**No submission made in this phase.** IndexNow POST is deferred to Phase 9, after push + live 200-verification, per the routine's standing rule (never submit a URL that isn't yet live).
