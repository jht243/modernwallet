# Cannibalization advisory — 2026-08-05 autocomplete-pass

No consolidation rows in this run's chart. Checked the 3 new-content routes against the existing sitemap for query-cluster overlap:

- `/guides/heloc-calculator-explained/` vs `/compare/heloc-vs-personal-loan/` and `/compare/cash-out-refinance-vs-heloc/` — **not competing**. The two existing pages are head-to-head comparison content ("HELOC vs X"); the new page is a standalone calculator/mechanics explainer (draw period, repayment, interest-only math) with no comparison angle. Different query intent, different SERP target. New page cross-links both comparisons rather than restating their content.
- `/guides/commercial-mortgage-calculator-explained/` — no existing page on this site covers commercial (non-residential) mortgages. No overlap.
- `/guides/auto-lease-buyout-calculator/` — no existing page covers lease buyout math (all existing `/auto-loan/` spokes are loan-payoff/amortization content, a different product from a lease). No overlap.

All 11 body-update rows this run add FAQ/section content to a single existing page each — no second page competes for the same query, so no cannibalization risk from these edits.

**0 competing pairs flagged. No manual-consolidate or differentiate action needed this run.**
