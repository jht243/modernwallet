# Phase 1 — Cannibalization detection (advisory only) — 2026-09-23

0 competing pairs detected.

This run's 4 new-content routes were each checked against every existing page in the same
cluster before being added to the chart (see "Shipped-work dedup" in `2026-09-23.md`):

- `/debt-snowball/` (spec only, not yet a live route) — nearest existing page is
  `/compare/debt-snowball-vs-avalanche/` (text comparison, no calculator) and
  `/credit-card-payoff/` (single-balance amortization). Neither targets "debt snowball
  calculator" as its head term or offers multi-debt ordering, so this is additive, not
  competing. No pair flagged.
- `/guides/free-cash-flow-calculator-and-dcf-valuation-explained/` — nearest existing page is
  `/operating-cash-flow/`, which explicitly measures a different metric (OCF, not FCF) and
  does not target "free cash flow" anywhere in its title/H1/FAQ. No pair flagged.
- `/guides/529-plan-tax-deduction-by-state/` — nearest existing page is
  `/529-savings-calculator/`, which targets "529 savings calculator" (a growth-projection
  tool) and never discusses state tax deductions. No pair flagged.
- `/guides/hysa-vs-roth-ira-vs-brokerage-account/` — nearest existing pages are
  `/compare/hysa-vs-cd/`, `/compare/hysa-vs-money-market/`, and `/compare/custodial-roth-ira-vs-hysa/`
  (scoped to accounts for a minor child). None target the general adult HYSA-vs-Roth-IRA-vs-brokerage
  question. No pair flagged.

The 7 body-text rows all add FAQs to their own already-correctly-targeted page — no second page
competes for the same query, so no pair applies there either.

No source file changed by this phase. No canonical, 301, body edit, or link re-point was made
here — this file is a detection record only, per `phase-1-consolidation.md`.
