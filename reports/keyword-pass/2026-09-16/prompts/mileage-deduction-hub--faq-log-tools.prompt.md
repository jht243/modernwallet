# Section: FAQ — mileage log / tracking tools

target page: MILEAGE_DEDUCTION_HUB (`/mileage-deduction/`), `src/data/se-hubs.ts`
task: Write ONE new FAQ answer (no heading — this is an FAQ answer, not a section) to add to this page's `faqs` array. Do not repeat the page's existing FAQs (see the page's current text above). This answers the row: "mileage reimbursement calculator excel" (+2 variants: excel mileage calculator formula, mileage reimbursement calculator map) — the reader wants a spreadsheet-style or map/route-based way to log mileage rather than doing the math by hand.
register: operator (matches the rest of this page)
page type: hub FAQ addition — no separate word floor, but the answer should be 2-4 full sentences, self-contained (an extractor will quote it alone).

## FAQ question (use verbatim)
"Is there a mileage log spreadsheet or app I can use instead of doing the math by hand?"

## CLOSED FACT LIST — state ONLY these facts. Anything else, you do not know.
1. This site has a free, downloadable mileage log template in CSV format (opens in Excel, Google Sheets, or Numbers) at /templates/mileage-log-template/. It has columns for date, start location, destination, business purpose, odometer start and end, and business miles, with two sample rows to delete before use. It is dated by row specifically so miles can be split correctly between 2026's two IRS rate periods (72.5 cents through June 30, 76 cents from July 1).
2. Route/GPS-based mileage-tracking apps that log automatically when you start driving are a real alternative to a manual spreadsheet — they capture trip-level data (date, start/end location, distance) without you remembering to write it down. Describe this generically; do NOT name any specific commercial app or vendor — no verified claim about any particular app's accuracy, price, or feature set is available.
3. Whichever method is used, the IRS still requires the same four things for a contemporaneous mileage log: date, business miles, destination, and business purpose for each trip.
4. Do NOT claim this site's calculator itself exports or generates a log — the calculator computes the deduction from miles you enter; the downloadable template (fact #1) is the separate log-keeping tool.

## CLOSED URL LIST
(none — this answer uses only the internal link below, no external citation needed)

## Internal links allowed
- /templates/mileage-log-template/ — the site's own downloadable CSV mileage log template (use this as the primary answer to the "excel"/spreadsheet part of the question)

## What NOT to claim
- Do not name any third-party GPS mileage app by brand (MileIQ, Everlance, Stripe, etc.) — not verified, not licensed to endorse.
- Do not claim a live "map" feature exists on this site's calculator.
