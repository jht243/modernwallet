---
keyword_pass_date: 2026-09-06
source: live competitor gap (Lens 1, DEGRADED — see below) + whitespace/live-market exploration (Lens 2), diffed against reports/seo-research/target-keywords.md
generated_by: keyword-gap-pass-auto build phase — SEMRUSH (403, dry key) + Ahrefs MCP (workspace quota 99,981/100,000 — treated as exhausted per demand-ladder rule) + Google Autocomplete exploration (6 seeds) diffed against existing pages
status: ready-for-execution
---

# 2026-09-06 Keyword Gap Pass — Keyword Chart

## Competitors this run

SEMRUSH returned `HTTP 403 Forbidden` on every call this run (`domain_organic_organic` for the live-competitor refresh, and `domain_organic` for each seed competitor) — the key is dry/invalid, not merely rate-limited (confirmed with a direct out-of-band call, same 403). Per the keyword-demand-ladder, a dry key must never block the pass, so the run demoted: Lens 1 (competitor-gap via SEMRUSH `domain_organic`) has no equivalent implementation on the next rung (DataForSEO's `keyword_data.py`/`dataforseo.py` helpers expose phrase-level volume/expand only, not per-domain ranked-keyword extraction), so **Lens 1 did not run this pass**. Before falling back to Ahrefs MCP for the same job, `subscription-info-limits-and-usage` was checked first: workspace usage is **99,981 / 100,000 units** (19 units of headroom) — the same "API units limit reached" condition the demand-ladder names as an automatic skip condition — so Ahrefs was **not** called, to avoid zeroing out the shared workspace quota other routines in this fleet also depend on.

The seed competitor list in `scripts/semrush_keyword_gap.py` (`calculator.net`, `bankrate.com`, `nerdwallet.com`, `smartasset.com`, `investopedia.com`, `mortgagecalculator.org`, `ramseysolutions.com`, `fidelity.com`, `themodestwallet.com`) is **kept unchanged** — no live refresh was possible this run, so nothing was added or dropped. The `COMPETITORS` constant was not touched (there is nothing new to sync).

- `calculator.net`, `bankrate.com`, `nerdwallet.com`, `smartasset.com`, `investopedia.com`, `mortgagecalculator.org`, `ramseysolutions.com`, `fidelity.com`, `themodestwallet.com` — kept, unable to refresh (SEMRUSH 403 / Ahrefs quota exhausted)

## Chart

**0 actionable rows.** Lens 2 (Google Autocomplete exploration via `.claude/tools/autocomplete-paa/autocomplete.py --mode all`) ran on 6 fresh seeds not mined by any prior pass — `self employment tax calculator` (232 suggestions), `s corp tax calculator` (67), `living trust cost calculator` (10), `prenup cost calculator` (0 — Google returned no completions), `long term care cost calculator` (34), `offer in compromise calculator` (20) — 363 raw suggestions total, all appended to the Autocomplete Discovery Log in `reports/seo-research/target-keywords.md` for future-run dedup.

Every genuine content cluster surfaced by these seeds was checked against the live page it would target and found **already substantively shipped**:

- `living trust cost calculator` cluster (how much does a living trust cost / average cost of creating one / cost to maintain) → `/estate-planning/living-trust-cost-calculator/` already carries the FAQ **"How much does a living trust cost?"** verbatim, with the exact $1,500–$10,000+ figures by trust type.
- `offer in compromise calculator` cluster (acceptance rate / $205 fee / example) → `/tax-resolution/offer-in-compromise-calculator/` (`spokes-tax-resolution.ts`) already carries FAQs **"What is the acceptance rate for an offer in compromise?"** (21.4%, FY2025 IRS Data Book) and **"What is the $205 application fee...?"**, plus a full worked example.
- `long term care cost calculator` cluster (average cost per day/month/year, how much to save) → `/elder-care/long-term-care-cost-calculator/` already answers **"How much does long-term care cost in 2026?"** with per-year figures by state and care type, and the calculator's own funding-gap analysis computes the savings target directly.
- `self employment tax calculator` cluster (deductions, Schedule C treatment, quarterly payments, vs. W-2, vs. S-corp) → `/self-employment-tax/` already models the QBI deduction and quarterly 1040-ES payment; `/compare/1099-vs-w2/` already covers the W-2 comparison in depth; `guides-self-employed.ts` already carries a direct self-employment-tax-vs-S-corp FAQ.
- `s corp tax calculator` cluster (by state / excel / free / california / texas / florida) → low-differentiation SaaS-style modifier queries (a searcher wanting a downloadable spreadsheet, not an interactive tool). The one candidate metadata row (work "by state" / "spreadsheet" phrasing into `src/data/se-hubs.ts`'s `s-corp-tax` entry) failed the metadata-rewrite cooldown gate — that file's metadata fields were last touched **1 day ago** (< 7-day hard cooldown), which routes the candidate to body-text — and a forced body section for a low-value modifier cluster isn't a good use of that reroute. Excluded rather than padded in.

**Off-mission exclusions** (not itemized as rows, per the chart's own rule): ~140 international/foreign-jurisdiction variants (UK, Canada, Ireland, Australia, South Africa, India, and 20+ other countries' self-employment/S-corp/estate tax rules) and ~90 US-state-name modifier variants of the SE-tax and S-corp seeds (the underlying tax mechanics don't vary by US state the way the query implies) — this site is USD/US-federal scoped, consistent with every prior pass's treatment of the same pattern (see 2026-09-02's auto-loan-by-country exclusions).

No `create new content`, `update existing metadata`, or `update existing body text` row survives this pass. This is a **Success Without Changes** run, not a tooling failure: Lens 2 ran on real, fresh seeds and returned real data — the data simply confirmed this site already covers the intent behind every genuine cluster found.

## Per-keyword export

See `reports/keyword-pass/2026-09-06.keywords.csv` — 0 actionable rows (header only).
