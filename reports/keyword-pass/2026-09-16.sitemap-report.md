# Phase 7 — Sitemap + IndexNow prep — 2026-09-16

## Sitemap
This repo has no static sitemap XML file (`@astrojs/sitemap` in `astro.config.mjs` auto-generates it at build time from actual page routes, filtered by `src/data/noindex.ts`). No manual sitemap edit is possible or needed.

Verified the 9 new routes will be picked up automatically:
- `src/pages/guides/[slug].astro` calls `getStaticPaths()` over the `GUIDES` array (`src/data/guides.ts`) — the 8 new guide entries are included automatically.
- `src/pages/[category]/[slug].astro` resolves spokes via the `SPOKES` aggregate (`src/data/spokes.ts`), which already imports `FREELANCE_RATE_SPOKES` from `src/data/spokes-freelance-rate.ts` — the new `creative-freelancer-rate-calculator` entry is included automatically.
- None of the 9 new slugs appear in `src/data/noindex.ts`.
- `lastmod` is assigned by the sitemap integration at build time (this integration does not read a per-page `updated` field), so every URL present in the sitemap at build time carries today's build timestamp — no manual lastmod bump needed or possible.

## IndexNow URL list
Written to `reports/keyword-pass/2026-09-16.indexnow.txt` — 15 URLs:
- 9 new routes (Phase 3)
- 6 substantively-changed existing pages (Phase 5 FAQ additions that also got a Phase 6 link splice, so they count as substantive per the "both a substantive edit AND a link = substantive" rule): `/zakat/`, `/mileage-deduction/`, `/freelance-rate/`, `/investing/`, `/mileage-deduction/irs-mileage-rate-2026/`, `/investing/compound-interest-calculator/`

**Excluded** (link-only touch, Phase 6, no lastmod/IndexNow bump per the rule): `/guides/is-extended-car-warranty-worth-it/`, `/guides/how-to-buy-a-car/`.

No consolidation losers this run (Phase 1 was advisory-only, 0 pairs). No legacy `.consolidation.txt` file exists for 2026-09-16.

**No submission made in this phase.** IndexNow POST is deferred to Phase 9, after push + live 200-verification, per the routine's standing rule (never submit a URL that isn't yet live).
