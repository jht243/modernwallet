# Task
Add ONE new FAQ entry to the existing FAQ list. Do not repeat any existing FAQ's content.

Question (use verbatim): "Can I calculate the cash conversion cycle in Excel?"

Answer must: confirm this calculator page already does this calculation for free online, then give
the exact three-step manual formula for a reader who wants to build their own spreadsheet:
DIO = (average inventory / COGS) × period days; DSO = (average receivables / revenue) × period
days; DPO = (average payables / COGS) × period days; CCC = DIO + DSO − DPO. 2-4 sentences.

## CLOSED FACT LIST
- This exact formula and the worked example ($90,000 avg inventory / $600,000 COGS / 365 days =
  54.8 days DIO; $110,000 avg receivables / $1,000,000 revenue / 365 days = 40.2 days DSO;
  $70,000 avg payables / $600,000 COGS / 365 days = 42.6 days DPO; CCC = 52.4 days) already appear
  on this exact page — reuse the SAME worked example numbers, do not invent new ones.

## CLOSED URL LIST
(none needed — no new external source required for this addition)

## Output format
Markdown, NO `## ` heading (this is a single FAQ answer, not a section) — just the answer prose,
2-4 sentences.
