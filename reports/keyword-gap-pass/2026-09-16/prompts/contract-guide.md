Return ONE JSON object with EXACTLY these keys, matching this repo's `Guide` TypeScript
interface (`src/data/guides.ts`). No extra keys, no markdown fences, no commentary outside the
JSON object.

```
{
  "slug": "<the exact slug given in the prompt>",
  "title": "<SEO title, 50-60 characters, includes the primary keyword>",
  "metaDescription": "<150-160 characters, includes the primary keyword, states the direct answer>",
  "h1": "<Title Case noun-phrase headline, includes the primary keyword, no two-clause formula>",
  "cardBlurb": "<one sentence, 20-30 words, summarizes the page for a guide-index card>",
  "introText": "<2-4 paragraphs separated by \\n\\n. First sentence is the direct answer to the
      page's question. If operator register and a tee-up belongs, it is the NEXT sentence, in
      the FIRST paragraph only, naming ModernWallet once: 'At ModernWallet, we...'. Weave in
      real internal links as markdown [text](/route/) using ONLY the routes given in the prompt.>",
  "sections": [
    { "heading": "<Title Case noun-phrase H2, no keyword-opening on more than one heading total>",
      "body": "<2-4 paragraphs separated by \\n\\n. Cite sources inline as markdown links using
          ONLY the URLs on the closed URL list, first mention of any organization linked.>" }
    // 5-8 sections total for an explainer-floor guide; use the section-by-section coverage list
    // in the prompt as your outline (you may combine or reorder for narrative flow, but cover
    // every point listed).
  ],
  "faqItems": [
    { "question": "<verbatim or near-verbatim from the prompt's FAQ list>",
      "answer": "<2-4 sentences, leads with the direct answer, cites a source link on first
          mention of any claim needing one>" }
    // use exactly the FAQ questions given in the prompt, in the order given
  ]
}
```

Notes specific to this repo's shape:
- There is no `subtitle`, `ctaTitle`, `ctaText`, or `ctaButton` field in this repo's Guide type —
  do not invent one; omit reasoning about a CTA block entirely and just end the last section (or
  intro) on the concrete next action per the content standard.
- `tools` and `sources` are NOT generated by you — the agent adds those separately from the
  prompt's tool/source list after your draft comes back. Do not include them in your JSON.
- Body word floor: reader-facing prose only (introText + every section body + every FAQ answer).
  Meet the floor given in the prompt with real substance, never padding.
- Every external link must come from the CLOSED URL LIST in the prompt. Every internal link must
  come from the INTERNAL LINKS list in the prompt. Never invent a URL.
