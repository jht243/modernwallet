# Bing Webmaster pass — 2026-08-21 — BLOCKED

## Blocker

The Bing Webmaster property for `themodernwallet.com` is registered under this API key's account but **still not verified** (same blocker as the 2026-08-14 run, one week ago):

```
GetSitesInfo → { "Url": "https://themodernwallet.com/", "IsVerified": false }
```

`resolve-site --base www.themodernwallet.com` still matched the host and returned a `SITE` value (host-matching does not check verification), but the data-pull call rejected with HTTP 400 `NotAuthorized` (`ErrorCode 14`):

```
GetCrawlStats failed: HTTP 400: {"ErrorCode":14,"Message":"ERROR!!! NotAuthorized"}
```

Bing requires the property to be **verified** (DNS TXT, meta tag, or XML file), not merely added, before granting read access to `GetCrawlStats`, `GetCrawlIssues`, `GetRankAndTrafficStats`, `GetLinkCounts`, `GetBlockedUrls`, `GetQueryParameters`, `GetFeeds`, `GetUrlSubmissionQuota`, etc.

## No findings, no fixes

No technical findings could be pulled or classified this run (second week in a row). No fixability matrix, no manifest, no fixes, no commits.

## Action needed

Verify `https://themodernwallet.com/` in Bing Webmaster Tools (https://www.bing.com/webmasters). The same DNS verification code is still on file for this property:

```
8d8ee7c1cba58b8b1549237e58229a13.themodernwallet.com
```

Add that as a DNS TXT record (or re-verify via the meta-tag/XML-file method), then the next scheduled run of this pass will pull live crawl data normally.

No raw JSON was saved this run (the very first data-pull call failed, so `deep-dive` was not invoked).
