---
status: ready-for-execution
---

# Bing Webmaster pass — 2026-09-25 — themodernwallet.com

**Site:** `https://themodernwallet.com/` (verified)

## Fixability matrix

| # | Finding | Source field | Bucket | Target | Proposed fix | Auto/flag |
|---|---|---|---|---|---|---|
| — | none | — | — | — | — | — |

No rows. Every diagnostic surface came back clean or empty:

- **`crawlIssues`** — `[]` (0 items). `GetCrawlIssues` reports zero per-URL crawl problems.
- **`crawlStats`** (65 daily rows, through 2026-09-23) — `Code4xx` noise of 0–4/day and `CrawlErrors` of 0–6/day (single small blip on 2026-09-20, already recovered by 2026-09-21) against 250–398 `CrawledPages`/day; no `Code5xx`, no `BlockedByRobotsTxt`, no `DnsFailures`, no `ContainsMalware`, no `ConnectionTimeout` on any day. `InIndex` climbed from 474 to 504 over the window — healthy growth. No sustained spike and nothing traceable to a per-URL row in `crawlIssues`, so not actionable (no invented data).
- **`blockedUrls`** — `[]` (0 items). Nothing accidentally blocked.
- **`queryParameters`** — `[]` (0 items). No normalization defects.
- **`linkCounts`** — `{ Links: [], TotalPages: 0 }`. Bing still has no inbound-link distribution data recorded for this property, so orphan-page detection (bucket 8) has no signal to classify against this run — 0 rows, not invented.
- **`feeds`** — sitemap index (`https://www.themodernwallet.com/sitemap-index.xml`) + sitemap-0.xml, both `Status: Success`, `UrlCount: 601` (up from 554 on 2026-09-18), most recently crawled 2026-09-23. Healthy — no re-submit needed.
- **`urlSubmissionQuota`** — `DailyQuota: 100`, `MonthlyQuota: 600`. Unused this run (nothing to submit).

## Working as intended / exclusions

None to record — no findings entered the matrix.

## Already-fixed (informational)

None — no technical findings existed in the prior report (2026-09-18) to re-check; that run was also crawl-clean.

## Conclusion

Site is crawl-clean this run. 0 fixable technical rows, 0 flagged-for-human rows. No manifest to approve; Phases 1–7 are skipped.
