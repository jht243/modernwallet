---
status: ready-for-execution
---

# Bing Webmaster pass — 2026-09-04 — themodernwallet.com

**Site:** `https://themodernwallet.com/` (verified)

## Fixability matrix

| # | Finding | Source field | Bucket | Target | Proposed fix | Auto/flag |
|---|---|---|---|---|---|---|
| — | none | — | — | — | — | — |

No rows. Every diagnostic surface came back clean or empty:

- **`crawlIssues`** — `[]` (0 items). `GetCrawlIssues` reports zero per-URL crawl problems.
- **`crawlStats`** (45 daily rows, most recent 2026-07-21 → 2026-09-03) — steady `CrawlErrors`/`Code4xx`/`Code301` noise of 0–4/day against 200–314 `CrawledPages`/day, with `InIndex` climbing from ~350 to 376 over the window. No sustained spike, no `Code5xx`, no `BlockedByRobotsTxt`, no `DnsFailures`, no `ContainsMalware` on any day. This is below-noise-floor and not traceable to any per-URL row in `crawlIssues`, so it is **not actionable** (no invented data).
- **`blockedUrls`** — `[]` (0 items). Nothing accidentally blocked.
- **`queryParameters`** — `[]` (0 items). No normalization defects.
- **`linkCounts`** — `{ Links: [], TotalPages: 0 }`. Bing still has no inbound-link distribution data recorded for this property, so orphan-page detection (bucket 8) has no signal to classify against this run — 0 rows, not invented.
- **`feeds`** — 1 sitemap index (`https://www.themodernwallet.com/sitemap-index.xml`), `Status: Success`, `UrlCount: 449`, `LastCrawled: 2026-09-02T18:13:40Z`, `Submitted: 2026-07-25T04:57:02Z`. Healthy — no re-submit needed.
- **`urlSubmissionQuota`** — `DailyQuota: 100`, `MonthlyQuota: 2700`. Unused this run (nothing to submit).

## Working as intended / exclusions

None to record — no findings entered the matrix.

## Already-fixed (informational)

None — no technical findings existed in the prior report (2026-08-28) to re-check; that run was also crawl-clean.

## Conclusion

Site is crawl-clean this run. 0 fixable technical rows, 0 flagged-for-human rows. No manifest to approve; Phases 1–7 are skipped.
