---
status: blocked
site: https://themodernwallet.com/
---

# Bing Webmaster pass — 2026-08-14 — BLOCKED

## Blocker

The Bing Webmaster property for `themodernwallet.com` is registered under this API key's account but **not verified**:

```
GetSitesInfo → { "Url": "https://themodernwallet.com/", "IsVerified": false }
```

`resolve-site --base themodernwallet.com` still matched the host and returned a `SITE` value (host-matching does not check verification), but every subsequent data-pull call in the `deep-dive` sweep rejected with HTTP 400 `NotAuthorized` (`ErrorCode 14`):

- `GetCrawlStats`
- `GetCrawlIssues`
- `GetRankAndTrafficStats`
- `GetQueryStats`
- `GetPageStats`
- `GetLinkCounts`
- `GetBlockedUrls`
- `GetQueryParameters`
- `GetFeeds`
- `GetUrlSubmissionQuota`

Bing requires the property to be **verified** (DNS TXT, meta tag, or XML file), not merely added, before granting read access to these endpoints.

## No findings, no fixes

No technical findings could be pulled or classified this run. No fixability matrix, no manifest, no fixes, no commits beyond this report.

## Action needed

Verify `https://themodernwallet.com/` in Bing Webmaster Tools (https://www.bing.com/webmasters). A DNS verification code is already on file for this property:

```
8d8ee7c1cba58b8b1549237e58229a13.themodernwallet.com
```

Add that as a DNS TXT record (or re-verify via the meta-tag/XML-file method), then the next scheduled run of this pass will pull live crawl data normally.

See `reports/bing-webmaster-pass/2026-08-14.bing-raw.json` for the raw error payload.
